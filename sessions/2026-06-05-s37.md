# Session 37 — 2026-06-05

### Map framing — staff engine confirmed
**Done:** Diagnosed why an OLD Wander order's FP1 map looked "off" in the staff engine vs a new order in the order form. Code paths are identical (same PNG asset via `assets/Template_Wander/`, same bleed-fit math: `bleed=3mm`, 200mm content, symmetric crop; engine canvas is a clean 600px with no border so no clientWidth asymmetry). Root cause = stale browser cache of the S36 re-exported PNG (re-exported under the SAME filename). Hard-refresh fixed it — Evgeny confirmed the engine now matches.
**Watch out for:** Re-exporting any asset under the same filename will silently serve the cached old image in already-open tabs. Hard-refresh (Ctrl+Shift+R) before judging a render. Old order age does NOT affect map framing — orders store only region+countries; geometry/asset resolve fresh from code + wander-data.js.

### Synced Wander CSVs → wander-data.js (caption fonts/sizes)
**Done:** Hand-synced font edits from `Wander_sizing_full.csv` + `Wander_Template_Sizing_Cover.csv` into `wander-data.js` (no CSV→JS generator — manual). Changes: SP0 H+V captions `NT Somic/16pt/medium` → `Cormorant Garamond/18pt/bold`; all 12 standard-spread captions (SP1/3/4/5/6) `16pt → 18pt`; cover spine `xMm 222 → 222.5` (with-bleed) + `16pt → 18pt`. Cover front already synced. Stale "NT Somic (SP0)" header comment removed. 85/85 tests pass.
**Decided:** Bumped cover spine to 18pt to match CSV even though that divergence pre-dated today — sync direction is CSV→JS.
**Watch out for:** SP0 no longer uses NT Somic at all. Cover spine `x` mid-task changed from 222 to 222.5 by Evgeny (he edited the CSV live). NOT committed/pushed yet — `wander-data.js` is dirty in the working tree.
