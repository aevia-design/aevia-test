# Session 128 — 2026-07-14

Joyride template build started (via /add-template, driven by docs/briefs/joyride-build.md —
THAT file is the build's memory, this log is the summary). Also committed the carried S127
promo fix first (`6306b1b`). Phase A stages 1–3 DONE with green gates; stage 4 (engine code
changes + smoke test) is fully specced but NOT started — session cut short for a fresh
context window.

### Scoping (before building)

- Ran /understanding-the-ask + /solutioning per owner request. Three genuine novelties vs
  Tender (the closest relative — Joyride's CSV is literally built on Tender's sheet):
  4-photo square cover, sized cover-caption boxes (title 52×43mm, maxLength 40 owner-set,
  autoShrink on overflow), and the SP4/SP8-right M pages (fixed mixed orientation, vertical
  photo overlaps horizontal ON TOP).
- **M-page allocation decision** (owner-approved): window-local V/H matching from the
  spread's own contiguous photo window; fallback = crop-into-frame + placement-audit warn.
  NO new CSV column; zIndex flag lives in joyride-data.js only. Recorded:
  work/joyride-template/decision-m-page-allocation.md.
- **Fonts:** owner-dropped variable TTFs serve the browser but NOT the PDF (pdf-lib embeds a
  variable font's default instance → Mulish Light would print Regular). Solution: instanced
  static cuts from those same files with fonttools — browser and PDF now share identical
  outlines.

### Built (stages 1–3, gates green)

- `assets/Template_Joyride/joyride-data.js` — full data file; CSV coords confirmed
  box-centres; cover = back/spine/front at 200/28/200mm (spine width flagged as a question).
- `assets/fonts/Lora-Regular.ttf` + `Mulish-Light.ttf` (instanced); BOTH form fontkit
  ligatures → added to LIGATURE_FONTS. @font-face + font catalogs on engine and
  customer-preview; FONT_FILE_MAP entries in export-pdf.js.
- Registry `joyride` on all 3 surfaces (svgBase `…/SVG/`, Papercut pattern).
- Gates: data file parses (node), 202/202 tests, export-pdf.js loads.

### NOT done — stage 4 next (blocks owner eyeball)

The engine cannot render Joyride correctly yet: cover renders only slots[0] (1 of 4),
SP7–SP9 are unreachable (hardcoded SP1–SP6 lists), M pages would get a same-orientation
pair, intro title box + autoShrink don't render. Exact file:line to-do list with all five
code changes + the smoke-test spec: docs/briefs/joyride-build.md stage 4. Open issues
(live <text> in cover SVG, 28mm spine question, @letdorabe collab question, draft intro
copy) are logged there too.

### Resume

Say "continue the Joyride build" → /add-template resumes at stage 4 from the build doc.
After stage 4's smoke test passes, hand back for the owner's visual pass in the staff
engine (that's the Phase A milestone he's waiting for).
