# Session 66 — 2026-06-22

## Summary
chunk-023 (web-resolution previews) merged to main + pushed (`10e34af`) → now LIVE on Cloudflare. Diagnosed why June-21 egress was still GB-scale, back-filled 3 existing orders, and set up the protocol for two parallel sessions (67 + 68) to follow.

## What happened

### 1. Diagnosed the June-21 egress (3.59 GiB / €0.37)
Evgeny checked billing: June 21 Cloud Storage egress was 3.59 GiB (≈€0.37), almost all "Download Worldwide Destinations" — far from the near-zero we expected after chunk-023.

Root cause: **chunk-023 was split and only the backend half was live.** The `generateDerivative` function + `getOrder` `derivativeUrls` were deployed to Firebase (S65), but the frontend that *requests* derivatives (`derivativeUrls` consumption in `customer-preview.html` + `template-engine.html`) was branch-only — never merged to main. Verified with `git grep derivativeUrls main -- pages/` (nothing) vs the branch (both files). So every load through the live site ran main's frontend → full-res originals. ~3.5 loads of AEV-042's ~1 GiB ≈ 3.59 GiB. The S65 "verified working" was a **local** branch checkout, not the live deploy. (LEARNINGS 2026-06-22: a split feature isn't live until both halves are deployed.)

### 2. Synced Wander itinerary alignment + merged
- Evgeny eyeballed the 3 UX fixes on the branch — all good except the Wander itinerary left-align, which he reverted to `center` in `Wander_sizing_full.csv`. Synced `wander-data.js` line 343 `halign: 'left'→'center'` (CSV-source-of-truth rule). Committed both (`ff5a3cb`, `22e57de`).
- 116/116 tests, then merged `egress-web-res-previews` → main (`10e34af`, `--no-ff`) and pushed. Cloudflare auto-deploys.

### 3. Back-filled AEV-039/040/041
Derivatives only generate for NEW uploads. Re-triggered the function for 3 existing orders via an in-cloud round-trip copy (zero egress) — recipe in LEARNINGS 2026-06-22. Verified 1:1: 54/54, 52/52, 50/50 derivatives. AEV-040 previews 15.37 MiB vs 2.08 GiB originals (~135×). Temp prefix deleted.

### 4. Designed the parallel-session setup
Evgeny wants to work on two things at once: (67) add a new template, (68) chunk-024 server-side PDF. They collide on `scripts/export-pdf.js`. Resolution:
- 67 = build the template (code), in worktree `../aevia-template`.
- 68 = **design** chunk-024 (brief + ADR, no code), in worktree `../aevia-chunk024` — design phase touches zero code, so it's genuinely parallel-safe.
- Implement chunk-024 code only AFTER the template merges.
- Protocol: `checkpoint` (not `handover`) in 67/68; proper `handover` at merge time, sequentially. See STATUS.md "NEXT SESSION".

## Decisions
- chunk-023 is complete and live; legacy orders fall back to originals unless back-filled.
- Two parallel sessions via git worktrees; checkpoint-while-parallel, handover-at-merge.

## Watch-outs (also in STATUS.md)
- PDF still serves full-res originals by design — chunk-024 removes that leg.
- Don't run `handover` in 67/68 (would clobber STATUS.md).
- `.claude/settings.local.json` left out; `assets/mockup example/` untracked, pre-existing.

## Pending verification
- AEV-042 live Network check (URLs contain `/previews/`, ~100–300 KB).
- Billing June 23 (for June 22) should show near-zero screen-load egress.
