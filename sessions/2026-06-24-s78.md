# Session 78 — 2026-06-24

## Thread 1 — Design-system polish (SHIPPED, committed+pushed `665abe4`)
Closed the lower-priority items from the S77 `/design-review`:
1. **`order.html` folded into the Lora/Inter type system** — added the `type.css` link after its inline `<style>` (the last page on legacy Georgia/15px). Now renders Inter @ 17px. 0 console errors desktop + mobile.
2. **Optional-spread card heights** — cards were already equal within a page (grid stretch); the only outlier was Newborn's 3-line descriptions → tightened the two Newborn add-on descriptions to 2 lines (cards now 258px, matching Scribble/Papercut).
3. **Form-field styling unified** — the only two genuinely-styled forms are `order.html` (`.field`) + `our-artists.html` (`.form-field`); aligned `order.html` to the artist pattern (radius 4→3px, font 14→15px).
4. **`context/style-guide.md` → Fonts section rewritten** (was stale "system fonts only"): documents `type.css` as single source of truth, the load-order rule, in-book-fonts carve-out, shared form-field look. Updated `project_type_system` memory (order.html no longer "legacy").
- 122/122 tests. `ARCHITECTURE.md` needed no change (font refs are PDF/in-book only). Left `--accent-dk` and the gold `.tag` as-is (both "consider/optional", taste calls).

## Thread 2 — `/add-template` skill + Tender build (Phase A)
**Created `.claude/skills/add-template/SKILL.md`** — interactive, project-scoped skill orchestrating the recurring template-integration job. References the memories as source of truth (doesn't duplicate gotchas); asks 3 scoping questions; drives a 10-stage build in 3 test milestones (Phase A engine renders / B flow / C live), one stage-or-phase per run with user confirmation between; resumable via a build-state doc. Captured the now-canonical **`overlay_position` CSV column** mechanic and the CSV-delimiter trap.

**Ran it on Tender** (couples/wedding, Love collection, replaces **Vows**). Closest precedent = Newborn (Intro mechanic only). Font = **Parisienne** (cursive, HIGH ligature risk). Build state + open issues live in **`docs/briefs/tender-build.md`**.

### Phase A — engine render — CODE COMPLETE, awaiting user eyeball
- **Stage 1 — data file** `assets/Template_Tender/tender-data.js`: 7 standard spreads (Newborn geometry family) + 3 functional (Intro replaces SP0; Our story + Words = optional spreads). Cover ellipse→2-arc path clip. letterSpacing 0.01 (= CSV tracking 10). SP3 `overlayBelow`.
- **Stage 2 — fonts:** Parisienne registered on all 3 surfaces (@font-face + picker engine/customer, FONT_FILE_MAP pdf). Ligature check confirmed (21→18 glyphs, GSUB liga) → added to LIGATURE_FONTS (char-by-char PDF draw). 1 Regular weight sufficient.
- **Stage 3 — registry:** all 3 surfaces, key `tender` (no SVG subfolder), data-script tags. Added `Tender` to the local template selector.
- **Stage 4 — render:** smoke test 0 pageerrors / 41 canvases / cover present (new harness `qa/debug-tender-render.mjs`). **`overlayBelow` wired on all 3 surfaces** (engine 2463, customer-preview 1769, pdf 491) — verified 6 SP3 overlays drop to z-index 0.

### Fixed in the review loop (user eyeball pass 1)
- **Words spread swapped** to left=text / right=full-bleed photo (matches SVG names + Xenia example). User updated the CSV (`Left H`→`Right H`, text row → `Left`).
- **Cover blue placeholder** — cover SVG had `<rect fill="#c1d5ef">` hiding the photo opening → set to `fill="none"` (same as Newborn's #d8eaf0→none). ⚠ RE-APPLY if Xenia re-exports the cover.

### ⚠ Watch-outs / pending
- **Tender is UNCOMMITTED working-tree** (mid-build, not user-approved). Files: `tender-data.js`, `template-engine.html`, `customer-preview.html`, `export-pdf.js`, `Cover/Artboard 1.svg`, `qa/debug-tender-render.mjs`, `docs/briefs/tender-build.md`. The `/add-template` skill is also uncommitted.
- **Cover photo comes from `specialPhotos.cover`, not the main grid** — assign via the special panel to test.
- **Functional text vs SVG-baked text** — the FP SVGs already contain styled default text; confirm with Xenia whether customer/staff text REPLACES it (Phase B) or risks double text.
- **Spread-1 SVGs heavy** (5.72 MB) — watch the 8 MB PDF-drop limit at Phase B.

### ▶ NEXT SESSION (Session 79) — resume Tender Phase B
Resume the skill: type **`/add-template`** (or "continue the Tender build"). It reads `docs/briefs/tender-build.md`, sees Phase A awaiting eyeball, and continues. If the eyeball passed, move to **Phase B** (order form: Intro fields + Our story/Words optional spreads + photo pools; customer-preview parity; PDF parity incl. ellipse clip + overlayBelow + full-bleed). Then Phase C (Vows→Tender product page + Stripe, E2E, merge backend-first).
