### — Plan 08-01 — FP text panels
**Done:** Editable `contenteditable` text block on FP1 (Birthday wishes) and FP2 (Funny words) left pages. Centred, 60% canvas width, NT Somic 16pt, stores in `window.bookCaptions[spreadIndex][side]['textPanel']`.
**Watch out for:** Key is the string `'textPanel'` not a numeric slotIdx — different from slot captions which use integer indices.

### — AI caption button fix — image resize before upload
**Decided:** Resize photo to max 1200px before base64-encoding, not send raw blob.
**Why:** Raw uploaded JPEGs can be 10–20 MB; Firebase Cloud Function body limit is 10 MB. Button showed "…" then silently reset — the 413 error was swallowed by the catch block into dbLog only.
**Watch out for:** `urlToBase64` now draws to a canvas at max 1200px. Do not revert to raw blob fetch — large photos will silently fail every time.

### — Caption voice prompt — source of truth is caption-voice.md
**Decided:** `index.js` now reads `caption/caption-voice.md` at startup via `fs.readFileSync` instead of using an inline hardcoded string.
**Why:** The two had diverged — edits to the `.md` file were not being picked up. Future prompt changes only need to touch the `.md` file.
**Watch out for:** The `.md` file must be deployed with the function — it is bundled as part of the functions package, so this works automatically.

### — Caption voice rules added
**Done:** Added "Hard rules" section to `caption-voice.md`: no "A/An" opener, no trailing period, no trailing comma. Also removed trailing periods from all example captions — they contradicted the rule and the model was copying them.

### — Caption width widened
**Done:** `below` and `above` caption slots changed from 70% slot width (15% inset) to 90% width (5% inset). Reduces 3-line wrapping to 2-line max for typical caption lengths.

### — Upper-right caption text-align fixed
**Done:** `upper-right` position captions now get `text-align: left` applied explicitly. Previously inherited `center` from `.slot-caption`.

### — Previous captions sent to AI for diversity
**Done:** `collectExistingCaptions()` helper collects all saved captions from `window.bookCaptions` and sends them as `previousCaptions` array in the API request. Function uses last 8 to avoid repeating phrasing, structure, or opening words across the book.

### — /checkpoint skill created
**Done:** New skill at `~/.claude/skills/checkpoint/SKILL.md`. Appends timestamped decision entries to `sessions/YYYY-MM-DD.md`. Wired into handover and finalise-session as Step 0. Prime now reads two most recent session files after whats-next.md.

### — CSV as single source of truth + csv-to-template.js
**Done:** Wrote `csv-to-template.js` (Node.js) that reads `Scribble_sizing_full.csv` and regenerates `template-data.js`, preserving existing SVG paths via line-by-line regex extraction. Added `functional_photo` and `functional_text` columns to CSV to drive pool types and textPanel flags.
**Decided:** Template engine reads generic flags (`funnyWords: true`, `heartClip: true`, `pool: 'special'`) from config — not hardcoded Scribble-specific logic. Each template's `template-data.js` sets the flags; the engine interprets them.
**Why:** User correctly pushed back on "template-agnostic engine" — future templates (love, travel) will have entirely different FP rules. The engine must be generic; the data file carries the template-specific behaviour.
**Watch out for:** After editing the CSV, run `node csv-to-template.js` from project root to regenerate `template-data.js`. Script warns about any TODO SVG paths that need manual filling.

### — Scribble rename (was Toddler/Sprout)
**Done:** Renamed template everywhere — folder `Template_Toddler` → `Template_Scribble`, global `window.TODDLER_DATA` → `window.SCRIBBLE_DATA`, `template: 'toddler'` → `template: 'scribble'`, product page `sprout.html` → `scribble.html`, all references in collections.html, style-guide.md, CLAUDE.md updated. Created `assets/templates.json` catalogue. Committed and pushed.
**Decided:** Template name "Scribble" chosen after viewing screenshots of the rendered template (crayon doodles, chunky hand-lettered font, deep plum cover). "Crayon" rejected — not a natural word in Austrian/German context.
**Watch out for:** Template engine is local-only (`http://localhost:8080/pages/template-engine`) — never deployed to Cloudflare Pages. The public website lives at `https://aevia-test.pages.dev/pages/collections`.

### — Repo root cleanup
**Done:** Moved carousel screenshots → `assets/screenshots/`, test scripts → `scripts/`, critic review doc → `reviews/`. Config files (firebase.json, cors.json, etc.) stay in root — Firebase requires it.
