# Session 54 — 2026-06-17

Focus: continue the 3D book renderer (ADR 0005). Branch `mockup-3d-renderer` (NOT main, NOT pushed). Two pieces: (1) fixed the closed-book back-view shadow — SHIPPED/working; (2) attempted the open-book / inside-spread view — REPEATEDLY FAILED on geometry realism, paused by Evgeny to regroup. NOTHING committed this session.

### 10:xx — Closed-book back shadow fixed (GOOD)
**Done:** `prototypes/book-3d-render.html` — the ground shadow-catcher plane was hardcoded at `y=0`, which is the book's vertical MIDDLE (book is centred at origin), so the back view showed a grey slab floating at mid-height + a band where the plane cut through. Fixed: measure the book's bbox and drop the plane to its true base (`new THREE.Box3().setFromObject(book).min.y`). Book unmoved → front framing byte-identical.
**Why:** Shadow plane position, not the book, was wrong. Generic (reads base from any book) so it survives Scribble/Wander.
**Watch out for:** Don't re-pin the shadow plane to a constant y — derive it from the book bbox.

### 11:xx — Spread capture pipeline built (GOOD, reusable)
**Done:** `qa/capture-spread.mjs` — sibling of `capture-cover-wrap.mjs`. Logs into the LIVE engine, loads an order, screenshots a `.spread-pages` element (two 600×600 `.page-canvas`, ~1204×600) → clean two-page texture. Env: `QA_ORDER` (default AEV-037), `QA_SPREAD` (0-based index, default 4 — NOT 0, the first content spread is a single photo after a technical white page). Output `sessions/qa-runs/spread-newborn.png` (gitignored). Evgeny ran it successfully for AEV-037.
**Watch out for:** Needs the staff password — Evgeny runs it. Engine `.spread-pages` is the interior-spread element; the cover row uses `.cover-canvas` instead (so `.spread-pages` only matches real spreads).

### 12:xx — Open-book geometry FAILED — accept failure, regroup
**Done:** Added `createOpenBook()` to `book-3d-renderer.js` + harness `prototypes/book-3d-open.html` + `qa/verify-open.mjs`. Iterated the geometry ~4 times; Evgeny rejected every version as unrealistic. Left in a thin/seamless/near-flat state that still failed.
**Decided:** STOP and regroup tomorrow rather than keep thrashing the geometry. The open-book code is left in `book-3d-renderer.js` (createOpenBook + gutterShadowTexture helper) but is NOT good enough and NOT committed.
**Why — what was tried and rejected, so we don't repeat it:**
  1. Symmetric flat planes split L/R at the gutter → "too flat" + a visible seam line.
  2. Symmetric convex bow (each page humps up in the middle, `sin(πt)`) → "feels very off", wrong physics.
  3. Asymmetric stacks (left elevated, right lower) + monotonic curl diving into the fold + dark gutter wedge → "very unrealistic book geometry", looked like a stepped wedge; the dark wedge read as a "black line" (real books join seamlessly, no line); also "very thick".
  4. Thin + single continuous surface (no leaves, so no seam) + soft feathered gutter-shadow gradient + realistic ~9mm thickness → STILL rejected ("failed again").
**Evgeny's stated requirements for the open book (the spec for next time):**
  - SLIM — a 40pp book is genuinely thin; do NOT exaggerate thickness (the S53 closed-book HERO_THICKNESS idea was dropped here).
  - SEAMLESS gutter — an accurate continuous join, absolutely NO line/seam/dark bar in the middle.
  - REALISTIC geometry above all — the hand-tuned curves/asymmetry all read as fake. Xenia's `assets/mockup example/open book.jpg` is a manual, non-pro example (NOT a northstar) — it just proves "realistic hardcover, pages bow, left sits a bit higher due to the сгиб/fold".
**Watch out for:**
  - The procedural box+plane approach may be the wrong tool for a believable open book. Next session: REGROUP on approach before coding — consider researching how realistic open-book mockups are actually built (proper curved page-block as a lofted/extruded slab where top AND bottom follow the fold curve, not a flat box fighting a curved top plane; or a fundamentally different technique). Treat it as a fresh solutioning problem.
  - The closed-book view (front/spine/back) is DONE and good — only the OPEN view is unsolved.
  - `sessions/qa-runs/` is gitignored — `cover-wrap-newborn.png` + `spread-newborn.png` + `open-*.png` NOT committed; regenerate via the QA scripts (captures need the staff password).
  - Nothing committed this session; `createOpenBook` in the renderer is throwaway-quality — feel free to rip it out and restart.
