# Session 36 — 2026-06-05

Short session. Founder re-exported the EU region map (still unhappy with framing
after S35). Applied `/systematic-debugging` instead of more pixel-guessing —
measured the order-form render geometry directly and proved the clip is even.
Committed + pushed the new PNG. ⚠ Even framing confirmed by Evgeny in the ORDER
FORM ONLY — staff engine + customer preview NOT visually checked.

### The framing debug (did it properly this time)
**Hypothesis (Evgeny's):** PNG bleed is perfect/even; our render clips the frame
unevenly instead of removing the 3 mm bleed symmetrically.
**Method:** measured, didn't guess (the S35 trap). New diagnostic
`qa/inspect-map-dom.mjs` drives the order form to the FP1 map and reports the LIVE
DOM geometry: canvas rect, overlay rect, per-edge overhang/clip, PNG natural size.
**Evidence:**
- PNG = **4056×4056, perfectly square**. Flat-brown bleed ≈56 px (~2.85 mm) **even
  on all 4 edges** (read raw pixels via sharp). → Evgeny was right, bleed is clean.
- Live order-form render: canvas inner 253², overlay 260.6² (square), **clip per
  edge L 2.78 / R 2.80 / T 2.78 / B 2.80 px** — symmetric to 0.02 px. 0 leftover
  bleed-brown at edges.
- The only real asymmetry is the **decorative cartouche border** inside the bleed
  (left ~60 px vs other sides ~90 px) — that's the artwork (Atlantic sea runs up to
  the frame on the left), NOT our trim. Can't be fixed by clipping.
**Outcome:** hypothesis "render clips unevenly" **disproven** on the order form.
Asked Evgeny to re-test visually → he confirmed **"now it's good"** in the order form.
So the re-export itself resolved his concern; our trim math was already even.

### Committed + pushed
`0c92717` — re-export `FP 01 Map Left (EU).png` (3.8 MB, under Cloudflare 25 MiB)
+ `qa/inspect-map-dom.mjs`. Pushed to `main`; Cloudflare auto-deploys.
**Left uncommitted on purpose:** `.claude/settings.local.json` (personal),
`docs/briefs/wander-map-reexport-spec.md` (moot since S35 — can delete).

### ⚠ Watch-outs / carried
- **Even framing verified in the ORDER FORM ONLY.** Staff engine + customer preview
  share the same single `wander-data.js` map entry + identical clip math, so they
  *should* render identically — but Evgeny has NOT eyeballed them. Confirm next.
- `qa/inspect-map-dom.mjs` writes to `sessions/qa-runs/...` **relative to cwd** —
  run it from the repo root (`node qa/inspect-map-dom.mjs`), NOT from `qa/`, or it
  creates a stray `qa/sessions/` dir (cleaned up this session).
- Customer-preview + PDF map render still never E2E-tested with a real order (carried
  from S33).
