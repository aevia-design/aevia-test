### — Motif engine moved to main repo
**Done:** Copied `motif-engine/` from `.claude/worktrees/` to project root; added `README.md` and `.gitignore` (excludes outputs/, node_modules/, .env)
**Watch out for:** The JS files (generate.js, config.json, prompts/) were already tracked by git from the worktree — only README and .gitignore were new additions.

### — Kseniia onboarding setup
**Decided:** Kseniia gets her own Claude Code account (not shared with Evgeny); Replicate API token shared as a separate token created under Evgeny's Replicate account
**Why:** Shared Claude accounts violate ToS and cause usage limit conflicts; separate Replicate token can be revoked independently
**Watch out for:** Replicate token goes in `motif-engine/.env` — never committed. Kseniia needs to create this file manually after cloning.

### — CLAUDE.md and customer-journey-v1.md updated
**Done:** CLAUDE.md rewritten — removed Webflow reference, corrected live URL to aevia-test.pages.dev, updated folder structure, added Template engine and Motif engine sections, updated phase statuses. customer-journey-v1.md updated to reflect template engine as built, caption flow documented, Puppeteer PDF constraints noted, tech stack table updated.
**Watch out for:** customer-journey-v1.md still lists several open decisions (print house, preview delivery, Stripe mode) — these are genuinely unresolved, not stale.

### — FP addon UX in order.html: cancel + validation
**Done:** Added "Remove this page" button to every addon section (text + photo). Cancelled addons are hidden and excluded from validation and the Firebase call. Submit validates all active addons: photo addons require a photo, text addons require content.
**Watch out for:** `cancelledAddons` is a `Set` in module scope — resets on page reload. If a user reloads mid-order, all addons are active again. This is intentional for now (no persistence).

### — FP2 textarea replaced with word-row inputs
**Decided:** Individual labeled input fields (3 default, + Add up to 10) instead of a single textarea for Funny Words.
**Why:** Free-form textarea gave no structure — users could leave it blank or type anything. Individual rows make the expectation concrete and allow per-row validation.
**Watch out for:** Validation reads from `#fp2-word-list input` elements, not from a textarea. The `addon-text-fp2` id does not exist for FP2 — any code assuming a textarea with that id will break.

### — Plan numbering + roadmap alignment
**Decided:** Plans 10–12 added. Plan 09-01 confirmed done (RAW blocking + low-res warnings + AI captions all implemented). Plan 10-01 (bloom.html) aborted — FP selector already on scribble.html.
**Why:** User confirmed 09-01 was complete from prior sessions; bloom.html is irrelevant to the actual product flow.
**Watch out for:** Phase 10 folder in `.planning/phases/` is `10-engine-finalize/` (not `10-bloom-amendments/` which still exists as an empty/aborted folder).

### — Cover CSV sync reverted, state TBD
**Decided:** Attempted to extend csv-to-template.js to parse Scribble_Template_Sizing_Cover.csv and generate a `cover:` block in template-data.js. User stopped and reverted all cover-related changes.
**Why:** Cover currently renders correctly in the template engine via hardcoded data — user wants the approach preserved, not accidentally broken. CSV sync will be revisited when approach is clearer.
**Watch out for:** `window.SCRIBBLE_DATA.cover` is NOT defined in template-data.js. The cover renders because `renderCover()` in template-engine.html has its own hardcoded cover definition inline (not from SCRIBBLE_DATA). Do not assume cover data lives in template-data.js — it doesn't yet.

### — FP3.left.V and FP4.left.V SVG paths lost in revert
**Done:** Confirmed SVG files exist: `FP Spread 3/FP Toy 05 V L.svg` and `FP Spread 4/FP Steps 07 V L.svg`. Set correct paths in template-data.js manually. These fixes were lost when template-data.js was reverted to HEAD.
**Watch out for:** Running `node csv-to-template.js` again will produce TODO placeholders for these two variants. After running the script, manually set: FP3.left.V → `FP Spread 3/FP Toy 05 V L.svg`; FP4.left.V → `FP Spread 4/FP Steps 07 V L.svg`.

### — PDF export plan gated on user spec
**Decided:** Plan 12-01 (Puppeteer PDF) has an explicit STOP block requiring user to confirm bleed, crop marks, color profile, and PDF version before implementation begins.
**Why:** Print house requirements vary significantly — wrong assumptions here could produce unusable files.
**Watch out for:** Do not start implementing 12-01 without those answers. The plan documents exactly what to ask.


### — FP3/FP4 left-V SVG paths fixed (again)
**Done:** Set `FP Spread 3/FP Toy 05 V L.svg` and `FP Spread 4/FP Steps 07 V L.svg` in template-data.js. Files confirmed present under `assets/Template_Scribble/Spreads/`. Engine prepends `SVG_BASE = '../assets/Template_Scribble/Spreads/'` so paths in template-data.js need no prefix.
**Watch out for:** These paths were previously lost in a revert. They are now also produced correctly by csv-to-template.js (see below) — running the script will no longer clobber them.

### — csv-to-template.js now generates cover: block from CSV
**Done:** Extended csv-to-template.js to parse `Scribble_Template_Sizing_Cover.csv` and emit the `cover:` block in template-data.js. Previously the cover block was hand-maintained and out of sync with the CSV.
**Why:** User edited the CSV to fix font sizes (33pt/28pt not 35pt/30pt) and spine xMm (204.5), but nothing picked them up — cover rendered from hardcoded template-data.js values.
**Watch out for:** Two bugs fixed during implementation: (1) cover CSV has a duplicate `Page` header — fixed by using column index 0 (`_page` key) not header name. (2) serializer was missing commas between multi-slot arrays — `serializeSlot` now joins with `,\n`. The script ran silently before without commas and produced broken JS; this would have broken any spread with 2+ slots if the script had been run previously.

### — Cover caption styling toolbar (Plan 10-02)
**Done:** Floating toolbar appears above focused front-page cover captions (Year, Name). Controls: font family (NT Somic / EB Garamond), style pills (Regular/Medium for NT Somic; Regular/Italic for EB Garamond), font size stepper (1pt steps), line spacing stepper (0.05 steps), letter spacing stepper (0.02em steps).
**Decided:** Styles stored in `window.coverCaptionStyles[key]` separate from text content in `window.bookCaptions['cover']`. Applied via direct style mutation on the live element (no re-render needed), so focus is not lost while stepping.
**Watch out for:** Toolbar uses `mousedown` + `e.preventDefault()` on all buttons to prevent blur-before-click. Style overrides survive `rerenderCover()` calls but reset on page reload (no persistence yet).

### — EB Garamond Semi-Bold wired
**Done:** Downloaded `EBGaramond-SemiBold.woff2` from Google Fonts CDN to `assets/fonts/`. Added `@font-face` at weight 600 in template-engine.html. Added "Semi-Bold" pill to EB Garamond entry in `COVER_FONTS` array.
**Watch out for:** Font file must stay in `assets/fonts/` — it is not a system font and will silently fall back to Regular if the file is missing.

### — Caption toolbar extended to spine captions
**Done:** Removed `if (!capDef.rotate)` guard so spineName and spineYear captions also show the toolbar on focus. Toolbar positions to the right of the spine element (not above) to avoid overlapping the canvas.
**Decided:** Same toolbar, same controls — no separate spine-only toolbar needed.
**Watch out for:** Spine captions are rotated 270°; `getBoundingClientRect()` returns their visual bounding box, so `rect.right + 12px` places the toolbar cleanly to the right. If the spine sits near the right edge of the viewport the toolbar may overflow — not a current concern at normal zoom.
