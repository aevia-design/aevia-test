# Session 120 — 2026-07-11

**Type:** Side-quest (marketing tooling for Xenia). No product code. Shaping + research +
review; build deferred to a fresh session. Committed at handover.

## The ask
Give Xenia (experienced marketer, AI-sceptic, plain-chat user, no terminal ever) a real
sparring partner for developing Aevia's OWN marketing strategy — not the chat-window AI
that gives shallow answers and folds under pushback. Owner ran `/shaping`; long dialogue
with several owner pushbacks that materially improved the design.

## The concept (work/marketing-sparring-partner/concept_v1.md)
**She is the strategist; the AI is analyst + critic + council — never the strategy
author.** Her scepticism ("AI can't do strategy") is treated as correct and built around.
Three layers: methodology (curated Agent Skills), grounding (`product-marketing.md` built
BY Xenia via interview — NOT pre-written from the S111 brief, which the owner partly
disagrees with), discipline (folder instructions: evidence + cost cap + kill criterion on
any spend recommendation; assumptions ledger; backbone rule — hold positions under
pushback, concede only to better arguments and say what changed your mind).

## Verified platform facts (live tests by owner, 2026-07-10/11)
- Custom Agent Skills run on claude.ai chat, Desktop, AND Cowork (account-level setting).
- One-click install: Plugins → custom marketplace from GitHub URL — **worked live** for
  `coreyhaines31/marketingskills`. Plugins are PER-ACCOUNT (Xenia needs her own install).
- claude.ai chat sandbox does NOT persist skill-written files across conversations
  (magic-word test failed cleanly).
- **Claude Cowork (Desktop) + a local folder DOES**: skills fire, files land on real disk,
  new sessions read them (all three checks passed live). → Chosen surface. No terminal,
  no manual upload loop. Docs: `work/marketing-sparring-partner/research_v1.md`.

## The research arc (the meat of the session)
1. First skill-pack recommendation (Haines `marketingskills`, 57 skills) came from a
   FLAWED search — single query, popularity-ranked. Owner inspected the pack and pushed
   back (SaaS + agency-for-clients skew). Lesson logged: skills.sh = fuzzy name match,
   top-10, install-counts are CLI telemetry from a developer population — momentum, not
   merit, not fit.
2. **research_v2.md** (rubric-first redo: 6-item fit rubric defined BEFORE searching; 31
   registry queries, 121 repos surfaced, ~20 SKILL.md files actually read): verdict =
   adopt selectively + rewrite templates + author on evidence. Haines skew is REAL but
   LOCATED (in `pricing`/`customer-research` templates), not blanket. Best find overall:
   `rampstackco/claude-skills` `brand-discovery` ("status quo = the real competitor" —
   exactly Aevia vs the phone camera roll). Confirmed gaps with nothing usable anywhere:
   occasion/gift-purchase psychology; founder-scale zero-data discipline.
3. **critic-agent review** (critique_research_v2.md): research ACCEPT WITH CAVEATS (8+
   verbatim spot-checks held; no motivated reasoning). Build corrections adopted: cut v1
   from ~10 to ~5 flow-critical skills; occasion-psychology skill is a TRAP as planned
   (expert smells invented domain facts) → defer, deliver the lens via customer-research
   rewrite + a council seat; assumptions-ledger skill = speculative duplicate of the
   folder instructions → build only on observed need. Two load-bearing holes named:
   `marketing-plan` never assessed; council advisor dossiers never read.
4. **verification_v1.md** closed both: `marketing-plan` = REWRITE (6/12 — AARRR, fCMO-for-
   clients and funding stages are STRUCTURAL; Retention/Revenue hollow for one-shot €
   purchases; hard dependency on the SaaS `marketing-ideas` skill; keep the resumable
   INIT→REVIEW→FINALIZE state machine + scored current-state rubric + organic-first
   sequencing). Council dossiers = SOUND (correct attributions, spot-check of the most
   falsifiable Ogilvy claim verified; uncertainty flagged rather than invented).

## Owner corrections that changed artefacts
- **€95 pricing was a working assumption hardening into a fact** — owner's default is
  still €70 for a 40-page book; premium repositioning is an OPEN question for Xenia's
  strategy work. Fixed in `folder-instructions.md` + memory `project_pricing_marketing`.
  (Ironic catch: the exact assumption-drift the discipline layer is designed to prevent.)
- Xenia is NOT actually a Luddite — design for the best approach, not the safest
  (this pushback is what led from chat+manual-file-loop to Cowork+folder).
- No rageatc plugin for her v1 (redundant methodology + trigger noise; revisit later).

## FINAL BUILD PLAN (next session — fresh chat)
Repo `aevia-marketing-skills` (create PRIVATE, test marketplace sync; public fallback is
zero-risk — repo holds methodology only, no Aevia data). TWO plugins in one marketplace:
- **aevia-marketing-core** (Xenia's day-one install): `product-marketing` (adopt, swap
  SaaS examples), `marketing-council` (adopt verbatim incl. 12 verified dossiers),
  `aevia-marketing-plan` (REWRITE — the big piece), `customer-research` (REWRITE —
  occasion/relationship personas; keep confidence-scoring + proxy-walk).
- **aevia-marketing-extras** (built now, installed when wanted): `brand-discovery`,
  `copywriting`, `content-strategy`, `social` (light edits).
Build order: skeleton+manifests+README(attribution) → adopt tier → customer-research
rewrite → marketing-plan rewrite (producer+critic with `/writing-skills` as the standard
and verification_v1 as the spec) → update xenia-flow.md → owner smoke-tests install.
All sources MIT. CHANGELOG carries the upstream-drift review trigger (re-check Haines
after Xenia's first plan cycle).

## Housekeeping
- Deleted research working files (`fetched/` 30 raw SKILL.md copies, `sweep_raw.json`).
- Kept 7 docs in `work/marketing-sparring-partner/`: concept_v1, folder-instructions,
  xenia-flow (deliverables); research_v1, research_v2, critique_research_v2,
  verification_v1 (evidence).
- Codespace idle-stop killed one researcher run mid-flight (unrecoverable, relaunched
  with incremental-save instruction). Fix shown to owner: idle timeout ↑ at
  github.com/settings/codespaces (max 240 min); background agents do NOT count as
  activity — keep the tab connected during long runs.

## Resume (S121)
Fresh chat, build the repo per the plan above. Also still carried: the ENTIRE S119
pre-launch QA execution track (catalogue sign-off, photo zip extraction, calibration run)
— untouched this session.
