# Session 191 — 2026-09-23

**The backlog moved to Trello, and TO-DOS #89 is fully built.** The Retry button shipped, the
German order form stopped showing English messages, and the stranded-upload job was checked
against live data for the first time. #89 now waits only on two live checks by the owner.

Commits: `c7aa32a` (Retry), `d11cca3` (project state), `90007f4` (German messages), plus this
handover.

---

### ✅ Trello is the canonical backlog

Board **"Aevia"**: https://trello.com/b/HCrNJRN1/aevia — id `6ab38135b3aee497becc9753` (the
API's `idBoard` rejects the short link). Lists Backlog → Next up → In progress → Blocked → Done →
Dropped. Labels are the old TO-DOS "when" sections. Rules are in CLAUDE.md "Backlog board":
**owner owns priority, Claude owns status**; no WIP limit (owner: "2 was arbitrary").

- 68 TO-DOS rows imported; STATUS.md's 23-item next-steps list became cards **#118–#132** or was
  already a ticket, and that section of STATUS.md is now a pointer.
- New this session: **#133** German country names (owner's spec), **#134** one folder structure
  per template (needs its own session), **#135** Printsmarter status push (Blocked).
- **`node scripts/trello-snapshot.mjs`** regenerates TO-DOS.md at handover. Idempotent (checked:
  two runs, identical file). Header ends at the fixed line "Do not renumber." — an earlier
  version re-read its own output and stacked a notice per run.
- `trello.env` (repo root, gitignored) holds key + token. The owner first put it in
  `functions/`, which `firebase deploy` uploads wholesale — moved.
- ⚠ **Send Trello text as JSON via `fetch`, never `curl --data-urlencode`.** Two writes came out
  literally as `%23133 %B7…`. Fixed both.

### ✅ #89 piece 0 — Retry on the same order (`c7aa32a`)

After a failed upload the form used to re-enable Submit and say "submit again" — **which created
a second order and stranded the first.** Now a Retry button re-sends only the missing slots from
the Files still in memory, over the same signed URLs (valid 24h).

- Worker pool extracted into `uploadSlots(indices)`; first run passes every slot, so behaviour
  is unchanged. Retry sends every missing slot **including `neverAttempted`**.
- `confirmUpload` fires only when `uploaded.size === total` across all runs.
- Retry disabled synchronously on click, handler assigned not added; Submit stays disabled
  while Retry is offered.
- Server side needed nothing: `confirmUpload` already handles `upload_failed → new`.
- qa:order +4 cases, seen red first. **Mutation-tested**: skipping `neverAttempted` and removing
  the double-click guard each turned the suite red.

### ✅ German messages on the German form (`90007f4`, owner scope-add to #89)

The string table covered markup, but **every sentence built in JavaScript stayed English**:
upload errors, photo refusals, the photo counter, both low-res warnings, region names + the
travel-map message, sign-in cancelled. All now come from `order-strings.js`.

- `photoRejection(file, t)` — the English literals stay as fallback; a test checks both copies
  agree. `tn()` picks plural pairs (German needs "konnte"/"konnten" where English does not).
- Technical errors ("Failed to fetch", "Server error (500)") now show the generic message.
- ⚠ **The German was written by Claude.** Listed on #123 for the native read.
- Country names in the travel-map dropdown are still English → #133.

### ⏳ Stranded-upload job — checked live, flip not yet observed

Owner checked the Uploading filter: latest `uploading` order is 13 Aug, nothing flipped — so the
job cannot email a real customer. A fresh test order was stranded **with Chrome's request
blocking on `*://storage.googleapis.com/*`** (Request conditions panel; the URL pattern field
starts `*://*`). That produces `photo_failures` — **a closed tab would not**, it gives
`no_client_error` and no email. Owner reports back when it flips.

### Printsmarter

- AEV-071 = **#150933**, AEV-100 = **#150932** (Firestore), both in production — their product
  ids resolved. The quantity=2 scare was a false alarm; our payload hardcodes 1.
- They lost the postback URL. The status-push email is drafted on **#135**, **Blocked** until the
  contract and delivery provider are settled (owner). The URL is left off the card on purpose.

### Other

- Global `~/.claude/CLAUDE.md` gained "Stopping and continuing" (from the Opus 5.5 guidance).
  Model is now Opus 5.5; VS Code extension 2.1.280.

### Held back from commit — owner's call
`assets/about us photos/` (4 originals, 37 MB — **committing publishes them full-size on
aevia.at**), `assets/packaging/` + logotype SVG, 14 `qa/debug-*` one-offs (#131),
`.claude/settings.local.json` (proposed: stop tracking it).
