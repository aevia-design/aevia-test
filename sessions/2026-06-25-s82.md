# Session 82 — 2026-06-25

**TENDER MOCKUPS created and wired into the website.** Produced the full mockup set for the Tender template from a real Saved order (AEV-044) and replaced the placeholder line-art on the product page + collections card with real imagery. All changes are **uncommitted working-tree** pending Evgeny's visual eyeball.

## What happened

### Mockup pipeline run (Tender, order AEV-044)
1. **Captures (Evgeny ran — needs staff password):** `qa/capture-cover-wrap.mjs` + `qa/capture-spread.mjs` with `QA_ORDER=AEV-044`, `STAFF_PW` via `Read-Host`. Produced `sessions/qa-runs/cover-wrap-AEV-044.png` + 20 `spread-AEV-044-NN.png` + manifest. (Captures are the only egress leg — one downsampled pull of the order; steps below are local/zero-egress.)
2. **compose-all (Claude):** added `tender` to the `TEMPLATES` map in `scripts/compose-all.mjs`, then `node compose-all.mjs AEV-044 tender` → `mockups/AEV-044/` (20 open spreads + closed + back). **First pale/cream cover the pipeline has handled** (all prior were dark) — composited cleanly.
3. **web-mockups (Claude):** added a `tender` entry (order AEV-044; fp map `open-00-fpintro`/`open-07-fpstory`/`open-14-fpwords` confirmed from the manifest) to `scripts/web-mockups.mjs`, then `node web-mockups.mjs tender` → 7 WebPs (17–54 KB) in `assets/images/mockups/tender/` (hero, closed, spread-1, spread-2, fpintro, fpstory, fpwords).

### back.png backdrop → white (Evgeny request)
- The back composer used a greige backdrop (`{209,206,200}`, added S74 so pale covers stand out) while closed/open composers use near-white `#f0f0f0` (240) — so within the Tender set the hero looked greige but closed+spreads looked white.
- Made the back backdrop **env-overridable**: `scripts/compose-back.mjs` now reads `BG_GRAY` (one grey value), default unchanged at 209 (4 live templates untouched). Regenerated Tender's `back.png` with `BG_GRAY=240` + re-derived `hero.webp`. Cream cover still reads against white via the soft contact shadow.

### Website wiring (matched the finished Scribble/Papercut pattern)
- **`pages/tender.html`:** replaced placeholder gallery CSS with real gallery/thumbs/lightbox CSS; swapped the placeholder SVG box for hero `<img>` + 4 thumbnails; converted `.addons-list` (checkbox variant) → `.addons-grid` photo cards with `.addon-preview` lightbox triggers; added the `#lightbox` element; updated script (`xtra` now toggles the `.addon-add` pill Add↔"Added ✓"; added `pickThumb`/`openLightbox`/`closeLightbox`). `addons.css` was already linked.
- **`pages/collections.html`:** Tender card thumb SVG → `<img src=".../tender/closed.webp">` (mirrors built templates).

## Verification
- 122/122 unit tests pass.
- Headless (Playwright) on tender.html: thumbnail switch works, lightbox opens from gallery + add-on previews, add-toggle flips to "Added ✓" and sets `.on`, **0 console errors**.
- Both pages serve 200 on the local dev server.

## Watch-outs / open items
- **Uncommitted working tree.** Changed: `pages/tender.html`, `pages/collections.html`, `scripts/{compose-all,web-mockups,compose-back}.mjs`; new `assets/images/mockups/tender/*.webp`. `.claude/settings.local.json` left out as usual.
- **Closed-book spine label reads mirror-reversed** ("Our Wedding") on `closed.webp` — tiny at display size, present on other templates too. Flagged as optional polish, NOT fixed.
- **`tender-data.js` `mockupEdges` is still placeholder** (uniform cream `#fbf8f6`). Front/back read fine; the 9mm spine sliver could be resampled to the taupe band `#8a817a` if it ever reads off.
- **`BG_GRAY` override only applied to Tender.** If we ever want all templates on a uniform white backdrop, flip the default in `compose-back.mjs` and re-compose the others.
- **Carried from S81 (owner actions, still owed):** (1) redeploy the Cloud Run renderer with `--memory 8Gi` (picks up the S81 spine fix + SVG cache), then regen AEV-044 + eyeball the centred spine; (2) Tender Stage 9 E2E on a fresh order (good moment to actually drive `/add-template`); (3) submit the Our Artists form once on the LIVE site to confirm email lands in xenia@aevia.at (owed since S77).

## Next session (Session 83)
1. Eyeball Tender mockups locally (tender.html + collections.html); decide whether to fix the mirror spine label; **commit + push** the mockup work (un-blocks it onto Cloudflare).
2. Then resume Tender close-out: renderer redeploy (`--memory 8Gi`) → Stage 9 E2E via `/add-template`.
