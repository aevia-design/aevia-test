# Session 74 — 2026-06-23

Pre-demo plan continued: committed/pushed S73, then built the Papercut mockups and
wired them into the website. Two commits; branch work for the mockups.

## 1. Committed + pushed S73 (now LIVE on main)
`fd38976` — S73's Papercut polish reached `main` → Cloudflare auto-deploys. Five fixes
(heart-page layering, re-exported Spread-5 SVGs, removed false low-res badge) plus
`LEARNINGS.md`/`STATUS.md`/the S73 session log. `.claude/settings.local.json` left out.

## 2. Papercut mockups generated + wired into the website
Branch `mockup-3d-renderer` (where all the mockup tooling + prior Scribble/Wander/Newborn
wiring lives — STILL not merged to main). Merged `main` into it first (only STATUS.md
conflicted; took main's version).

**Pipeline (cost-checked):** capture downsamples every photo to ≤1600px before disk, so a
full per-order capture is ~tens of MB egress, one-time (~€0.01) — NOT the full-res originals
(those are read only by the in-region EU PDF renderer, never on the laptop). Confirmed in
`qa/capture-spread.mjs` (sharp resize in the GCS route).

**Steps run (commit `c8b91bb`):**
- Evgeny ran the two gated captures on **AEV-043** (`capture-spread.mjs` → 20 spreads +
  manifest; `capture-cover-wrap.mjs` → cover wrap). Claude can't run these (need staff pw).
- Registered Papercut in the pipeline: `cover.mockupEdges {front,spine,back}` all `#8bb8d8`
  in `papercut-data.js` (uniform blue cover); template entry in `compose-all.mjs` +
  `web-mockups.mjs` (FP positions 04/07/11/14/18 — identical to Scribble).
- `node compose-all.mjs AEV-043 papercut` → 22 mockups in `mockups/AEV-043/` (gitignored).
- `node web-mockups.mjs papercut` → 9 WebPs (33–49 KB) in `assets/images/mockups/papercut/`.
- Wired `papercut.html` (hero gallery + 4 thumbs + 5 functional-page previews w/ lightbox)
  and the `collections.html` card (closed shot). **Verified EXACT slot parity with Scribble**
  (same files/counts). Browser-checked both pages: 0 console errors.

## 3. `compose-back.mjs` fixes (shared composer — benefits all templates)
- **Clip back/front cover art to the book body** (`Pages`), matching the spine's existing
  clip. Stops cover decorations spilling past the trimmed edge onto the backdrop (the
  "floating pink shape" Evgeny circled). Clear win.
- **Warmer greige backdrop** `#f0f0f0`→`#d1cec8` (pre-grade `{209,206,200}`) on the BACK shot
  only, so a light cover reads as a distinct object instead of dissolving into white.

## ⚠ UNRESOLVED — back-cover top-left "white corner" (Evgeny deferred: "ok for now")
Root-caused but NOT fixed. The cover art has a `#f7e9e1` **cream border** (the bleed/page
margin) around the blue — present + correct in the capture (sampled `#f7e9e1` at the corner).
The mockup renders it **white** because:
1. the final brightness grade (`linear(1.07,-4)+brightness 1.05`, tuned for dark covers)
   pushes near-white cream (247) past 255 → pure white; and
2. the new edge-clip trims the outer cream rim.

**Open decision (the fork) for next time:**
- **A** — match the engine/PDF: render the cream border as cream (book sits in a thin cream
  frame; shows the *uncut* artwork).
- **B (recommended)** — match the *finished cut book*: trim the cream bleed margin before
  warping so the blue cover fills the book face edge-to-edge (no border, most photoreal).

Also deferred: propagating the greige backdrop to `compose-closed.mjs` + `compose-mockup.mjs`
so the thumbnail row is consistent (right now only the hero/back is greige).

## ⚠ Watch-outs / state
- **Mockups are NOT live.** All mockup work (this session + S63 Scribble/Wander/Newborn) sits
  on `mockup-3d-renderer`, never merged to main. The website-facing parts (the `.webp`s +
  product-page edits) MUST merge to main to appear on the demo site; the `compose/capture`
  scripts are offline tooling and only ride along harmlessly. **Merge decision still pending.**
- `c8b91bb` is committed on the branch (clean tree). `.claude/settings.local.json` + the stray
  untracked `sessions/2026-06-23-s71.md` left alone as usual.
- Owner still owes (carried from S72): a dashboard PDF run on AEV-043 + a billing check
  (egress near-zero; one-time ~€0.2–0.9 migration-copy charge expected).

## ▶ NEXT SESSION (Session 75)
1. **Decide the mockup→main merge** (brings ALL mockups live for the demo) — and/or resolve
   the back-corner fork (A vs B) + greige propagation first if you want the set polished.
2. Fix a few website UX things before the demo.
3. One full E2E test.
4. Record the demo per `docs/naitive-fellowship/walkthrough-script.md`.
