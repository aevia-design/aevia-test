# Session 22 — 2026-06-03

### 09:00 — chunk-018 wrap-up closed
**Done:** Confirmed chunk-018 commit `1a7a3fa` is pushed to origin/main; user deleted the dead Cloudflare Access app. STATUS.md updated to mark both done.

### 09:30 — QA automation: agentic discovery, not a scripted suite yet
**Decided:** Build automated customer-journey testing in two halves with different timing. NOW = cheap agentic discovery (Playwright drives, Claude judges screenshots). LATER = a scripted regression suite, deferred until after template digitisation (chunks 010–017) settles the order-form/engine UI and just before Stripe goes live.
**Why:** A scripted suite built now would target UI that chunks 010–017 will reshape — build-twice tax. No real customers yet = regression net has near-zero value today. Discovery has no carrying cost and pays off immediately.
**Watch out for:** Don't start building the full scripted regression suite until 010–017 lands.

### 09:45 — Browser tooling: only real Playwright works here
**Decided:** Use locally-installed `@playwright/test` + Chromium, driven via Node scripts in `qa/`. Claude reads the screenshots to judge.
**Why:** The `playwright-cli` binary the rageatc skill assumes is NOT installed and the npm package by that name is a dud. A subagent given the Playwright MCP tools FAKED a run (wrote markdown "test plans", never opened a browser) — those tools aren't functional in this env. User installed `@playwright/test` + chromium (in root package.json devDeps).
**Watch out for:** Do NOT trust a subagent to do browser testing here — MCP browser tools silently don't work and it'll fabricate results. Drive Playwright directly via Bash + Node scripts.

### 10:15 — Discovery run 01 (order front-leg) findings
**Done:** `qa/order-journey.mjs` walked scribble.html → order form → upload 51 DTS photos → submit. Created real order AEV-023. Screenshots + FINDINGS.md in `sessions/qa-runs/2026-06-03-run01/`.
**Decided:** One real product finding worth fixing — the Scribble configurator promises "approximately 36–45 photos" but order.html requires exactly 51 (configurator uses a rough BASE_PHOTOS range; order form uses calcPhotoTarget() counting real slots). Customer told one number, asked for ≥6 more mid-upload.
**Why (non-findings):** Empty configurator product images = intentional placeholders (photoshoot pending), per user — ignore. 53× net::ERR_ABORTED on GCS upload PUTs = instrumentation false positive (product code never reads the PUT response body); order succeeded with all files. One-time confirm later: AEV-023 has all 53 files in dashboard.
**Watch out for:** Cloudflare Pages serves clean URLs — order form is `/pages/order` not `/pages/order.html`; engine moved to `/pages/staff/template-engine`.

### 10:45 — Chain script built (staff→customer→pay), not yet run
**Done:** Wrote `qa/staff-customer-chain.mjs` — picks up AEV-023: staff login (engine #eng-email/#eng-pwd) → Order mode → load → screenshot loaded book → Save book state (#oip-save-btn) → dashboard generate preview link (button[onclick="generatePreviewLink('AEV-023')"], reads .preview-url) → customer-preview ?token= → #approve-btn → #pay-btn → Stripe test card 4242 → ?payment=success.
**Decided:** v1 = the spine only. Deliberately SKIP the fragile rich staff edits (swap spreads/photos, add captions via canvas) until the spine is proven; instead screenshot the loaded book and judge visually that customer captions/special pages/photos came through.
**Why:** Canvas drag/edit automation is brittle; prove end-to-end first. Stripe-hosted checkout is the likeliest failure point (selectors guessed: #cardNumber/#cardExpiry/#cardCvc/#billingName + .SubmitButton).
**Watch out for:** Staff password passed at runtime via `$env:STAFF_PW` (PowerShell `Read-Host`, never inline → not in shell history); script reads `process.env.STAFF_PW`, never logs/screenshots it. Firebase auth persists across same-origin staff pages in one browser context, so one login covers engine + dashboard.

### 11:30 — Chain script fixes (4 reactive patches, then stopped)
**Done:** Fixed chain script across several runs: (1) login — user mis-typed `Read-Host "Staff password"` putting their pw INSIDE the quotes (the quoted text is the prompt label, not a placeholder); (2) dashboard `goto` `networkidle`→`domcontentloaded` (dashboard holds Firestore listeners open, never idles); same for customer-preview; (3) bumped book-render waits (6s→14s engine, +caption-wait on customer); (4) Stripe checkout opens on a payment-method PICKER — must select the "Karte"/Card radio (custom component, force-click + verify card field appears) before filling; email field is required and empty on load.
**Decided:** Made the chain STATUS-AWARE — reads `window.orderData.status` and branches (skip approve if already approved, recognise paid). NOT a product change; the test adapts to existing product behaviour (approved orders disable #approve-btn + show #pay-btn via lockForApproval, customer-preview.html:898,935).
**Why:** AEV-023 is a stateful shared fixture each run mutated (sent→approved→…), causing a `#pay-btn` "not visible" race. systematic-debugging Iron Law: stopped selector-guessing after 3+ patches and READ the source instead.
**Watch out for:** `serve` 301-redirects `/x.html`→`/x` and DROPS the query string — to load customer-preview locally with a token use the clean URL `/pages/customer-preview?token=…` (no `.html`), else the token is lost and no order loads.

### 12:00 — "Empty book" Finding A: false alarm in engine, REAL in customer view
**Done:** Investigated the grey/empty photo slots in screenshots 07/08/11 via `qa/probe-photos.mjs` (loads public customer-preview, waits for pool, counts loaded imgs).
**Decided:** Initial probe said 51/51 pool photos loaded, 96/96 imgs OK → I called it a screenshot-timing false positive. LATER (during gate verification) discovered that "96 imgs" counted SIDEBAR THUMBNAILS, not book placements. AEV-023's CUSTOMER book genuinely renders EMPTY: `customerBookAssignments={}`, only 2 placed imgs in DOM, sidebar shows 51 unplaced.
**Why:** AEV-023's customer-side data is CORRUPTED — its `customerBookAssignments` was saved empty and `customerCaptions` lost the special-page text. Almost certainly the earlier automated chain runs clicked Approve → `saveBookState()` serialized `window.bookAssignments` before render finished populating it (timing race in the TEST script). Staff data (`staffBookAssignments`, `staffBookCaptions`) is intact and complete.
**Watch out for:** AEV-023 is NOT a clean fixture — its customer view is empty/corrupted. Don't trust it to verify the "allow a complete book" path. `probe-photos.mjs` "imgs loaded" counts sidebar thumbs too — not proof of book placement. Real but separate concern: customer approve/save can persist an empty/partial book that OVERRIDES the good staff arrangement on reload (precedence: customer wins, customer-preview.html:916-923).

### 12:30 — Incomplete-book guard: built + tested + partly verified
**Done:** New product feature — hard-block sending/approving an incomplete book. Shared tested helper `assets/js/book-completeness.js` → `checkBookComplete({bookAssignments, photoPool, requiredCaptions, bookCaptions})` → `{complete, reasons[]}`. 12 new jest tests (`tests/book-completeness.test.js`), full suite 69/69 green. Wired into: customer-preview approve (live block + `showToast(...,'warn')` matching the H+V swap message), staff engine save (computes completeness, stores `bookComplete`+`incompleteReasons`; save still allowed when incomplete — only SEND is gated), `saveStaffState` Cloud Function (persists `staffBookComplete`+`staffIncompleteReasons`), dashboard `generatePreviewLink` (blocks + alert when `staffBookComplete !== true`). Both render paths push `{spread,side}` into `window._requiredCaptions` wherever `variant.textPanel?.caption?.allowed` (mirrored engine + customer per parity rule).
**Decided:** "Caption required" = data-driven, identical to "an editable caption box is shown" (`variant.textPanel.caption.allowed`). Ordinary spread captions ignored. Three checks: empty slots, unplaced photos (non-duplicate), blank required captions. Send-gate = client-side (staff trusted); approve-gate = client-side per user (block button + message); server-side approve hardening deferred (only a devtools-hacking customer could bypass, hurts only their own order).
**Why:** User: saving incomplete is fine, but SENDING to client and APPROVING must be hard-blocked. Built test-first (TDD skill).
**Watch out for:** REQUIRES deploy: `firebase deploy --only functions:saveStaffState` — until then `staffBookComplete` is never written and the dashboard send-gate blocks EVERY order ("not saved as complete yet"). Gate VERIFIED to correctly BLOCK an incomplete book (AEV-023: "51 unplaced, 2 blank captions"). NOT yet verified: that it ALLOWS a complete book, and the staff send-gate round-trip — both need a FRESH clean order. Helper reads `window.bookAssignments`/`bookCaptions` = correct source (what renders + what saves).

---

# Session 24 — 2026-06-03 (afternoon)

### Placement-audit noise fix (user's "fix A")
**Done:** `logPlacementAudit` (`template-engine.html`) now flags **only backward moves** (`rk < prevRank`) instead of any deviation from `prevRank+1`. Reviewed the 80-page AEV-026 audit with the user: the only genuine reordering is the SP1 case (single-slot LEFT page + 2-slot RIGHT page, all same orientation → multi-slot side fills first and grabs the earliest photos, single slot gets the leftover later photo → reading order inverts). Everything else was either correct (H-on-its-own-right-page) or cascade noise. Old audit double-counted each inversion (a `skipped` + a `back`) and cascaded phantom skips → 34 "breaks" from ~6 real ones.
**Decided:** Only fix the audit (A), NOT the algorithm (B). User: the placement effect is minor ("photos are near each other anyway"). Backward-only flags collapse 34→~8, each manually checkable.
**Watch out for:** This is a local engine edit — needs deploy to reach live (bundled with pending #63 render fix).

### Downstream QA chain on AEV-026 (staff edits → approve → pay → PDF)
**Done:** Built `qa/downstream-chain.mjs` — a trimmed chain that SKIPS the engine load+save leg (user had already saved their creative edits in their own browser; re-saving headless re-risks the AEV-023 save-before-render corruption). It generates the preview link → inspects the customer-rendered book → approves → Stripe pays. Ran clean on AEV-026: **`pool:106 placed:106 spreads:40`** (no corruption), through `payment=success`. Console "errors" were all Stripe 3rd-party (Amazon Pay/Klarna CORS) — not our code.
**Decided:** Hand-off model confirmed: scripts headless, user does creative edits in their own logged-in browser, Claude runs deterministic legs + inspects. Script can't run via Claude's non-interactive shell (needs staff pw) → user runs it, Claude reads artefacts in `sessions/qa-runs/2026-06-03-downstream-AEV-026/`.
**Watch out for:** First chain run failed — old `photoPool>=51` wait gave up at ~67s on the slow 106-photo load (book was still on "Loading photos… 60 of 110"). Fixed: now waits for `#load-progress-label` to detach (true render-done signal), 300s timeout. Big-book customer load is genuinely slow (TO-DO #66).

### PDF generation for AEV-026 (`npm run pdf -- AEV-026`) — two fixes
**Done:** PDF built clean (cover + 80 pages, all 40 spreads, blank QR page), uploaded to GCS `AEV-026/pdfs/preview.pdf`. User visually confirmed cover renders correctly. Fixed two issues the user flagged:
1. **Local file should not be saved** — `export-pdf.js` order-mode now uploads `preview.pdf`/`print.pdf` straight from in-memory bytes to GCS, no local copy. `uploadAndSignPdf` takes bytes (was a path). Also removes the S21 cosmetic bug (printing a local path it didn't keep). Cleaned stale `pdf-out/` leftovers.
2. **"Strange symbol" after "WILD" on the cover** — NOT an emoji: a **non-breaking space (U+00A0)** in the title `"OUR WILD \nJANE"`. The contenteditable cover-title editor converts a space-before-a-manual-linebreak into NBSP; the embedded "NT Somic" print font has no NBSP glyph → `.notdef` box. Root cause: cover captions (`drawCoverCaptions`, line 740) read text raw via `.trim()`, bypassing `stripHtml` (which already normalises NBSP→space and is used by spread captions). Fix: route cover captions through `stripHtml`. Re-ran → uploaded fresh PDF; user confirmed clean.
**Decided:** `book-state.json` (GCS) is written by the engine's **Export** button, which is SEPARATE from **Save** (Firestore). User had only clicked Save → PDF failed initially. Logged as footgun TO-DO #64. Also made the export confirmation message persistent (was flashing "Exported ✓" for 3s then clearing — user couldn't tell it worked).

### Files changed this session (uncommitted)
- `pages/staff/template-engine.html` — audit backward-only flags; persistent export confirmation message. **Needs deploy** (+ carries pending #63 render fix).
- `scripts/export-pdf.js` — cover-caption NBSP fix via `stripHtml`; order-mode PDFs upload to GCS, no local copy. **CLI — effective immediately, no deploy.**
- `qa/downstream-chain.mjs` — new downstream QA script.
- `.claude/settings.local.json` — incidental.
- Untracked: `assets/Template_Wander/` (new template assets, not this session's work — flag to user).

---

# Session 25 — 2026-06-03 (Wander template kickoff)

### 15:30 — Wander template data file built (chunk-010 Phase 0)
**Done:** Wrote `assets/Template_Wander/wander-data.js` (parses clean): cover (2 free-text captions), SP0–SP6 (geometry identical to Scribble; colours/fonts per Wander CSV — Cormorant Garamond Bold captions in navy #262262, SP0 keeps NT Somic), FP1 map-page data contract, and `mapCoordinates` (183 countries → {region,xMm,yMm}, generated from Map_Coordinates_upd.csv via node — not hand-typed). Downloaded Cormorant Garamond static .ttf (Light/Regular/Medium/SemiBold/Bold) into `assets/fonts/`.
**Watch out for:** Wander SVGs sit directly under `Template_Wander/` (NO `Spreads/` subfolder, unlike Scribble) — the chunk-020 loader must use a per-template asset base. Cormorant is NOT yet registered in @font-face / font picker / export-pdf FONT_MAP (that's chunk-010 Phase 2).

### 15:35 — Cover caption model verified (don't change Scribble)
**Decided:** Kept Scribble's cover-caption convention as-is: `xMm`/`yMm` = box CENTRE (with-bleed), `wMm` = width, `align` positions text in box (right → ends at xMm+wMm/2), `hMm` stored but DORMANT (cover text vertically centres on yMm). Wander cover captions are FREE TEXT (`front`/`spine`, no name/year semantics like Scribble) — placeholder "e.g. Dolomites, 2025".
**Why:** User measured Wander coords from centre, same as Scribble, and likes how Scribble cover renders. Render code (engine `renderCover` ~L1623, PDF `drawCoverCaptions` L818-840) already supports wMm+align, so right-align Just Works.
**Watch out for:** Did NOT add the new width/height/alignment columns to Scribble's CSV — Scribble covers are all centred so the columns would be inert; no reason to risk a working file.

### 15:50 — Map functional page design locked (chunk-022)
**Decided:** Travel map page = LEFT page is the regional map (6 SVGs keyed by region code from the coords CSV's `Map` column), RIGHT page is a staff-formatted itinerary textPanel over `FP 01 Map Right.svg`. Pin = `GEO PIN.png`, CENTRE-anchored, 12mm W × 23mm H (calibrate later). Order form: country multi-select, NO photo upload, blocks cross-region selection (prompt to pick one region), shows which map will display. Reusable across all Travel-collection templates.
**Why:** Filenames ("Map Left (region)" ×6 + single "Map Right") settled the left/right question; coords CSV already carries region → no new ID columns needed.
**Watch out for:** Not built yet — engine/customer/PDF rendering of map+pins+itinerary is chunk-022. Open decision: cross-region UX (block vs warn) — current plan = block.

### 16:10 — Roadmap restructured + brief-per-chunk policy (chunks 020/022/010)
**Decided:** Added chunk-020 (multi-template engine seam — blocks 010–017) and chunk-022 (Travel map page); repurposed chunk-010 as "Wander template" depending on both; renumbered the duplicate Phase-4 chunk-018 (page-flip) → chunk-021. Brief policy: the roadmap chunk entry IS the brief for well-understood chunks; standalone briefs only for novel ones (e.g. chunk-022). Backing plan in `docs/briefs/wander-template.md`.
**Why:** First second-template carries one-time infra (engine is hardwired to SCRIBBLE_DATA) + the reusable map feature; the 2026-05-28 "all templates identical" assumption holds only for templates 3–9. Don't duplicate brief content the chunk format already captures.
**Watch out for:** Build order is chunk-020 → chunk-022 → finish chunk-010. chunk-020 has a hard "Scribble renders byte-identically" regression gate across engine + customer-preview + PDF.

---

# Session 26 — 2026-06-03 (chunk-020 built + verified)

### Recovered from a stale-checkout git mess
**Done:** Session opened on local `main` at S21 (6 commits behind origin/main, with a leftover unmerged `.claude/settings.local.json` conflict and an Excel-locked Wander CSV). Resolved the conflict (kept the superset allowlist), fast-forwarded `main` to origin/main (S25, `3980a20`) — all S22–S25 work confirmed present, nothing lost.
**Watch out for:** Everything was already committed+pushed last session; the confusion was purely a stale local checkout. If a session opens "behind origin/main", fast-forward before doing anything.

### chunk-020 — multi-template engine seam — DONE, committed `374af3d`
**Done:** Built the seam via developer-agent, reviewed via reviewer-agent (Accept), then **refactored from an `if 'wander'` literal to a `TEMPLATES` registry** on the user's steer ("matching shall depend on the product-form choice"). The seam now: lowercases the order's `templateName` and looks it up in a registry → `{data, svgBase/assetBase}`; falls back to Scribble for unknown/missing. Mirrored across staff engine, customer-preview, and `export-pdf.js` (parity held; only `../` vs `../../` differs). 73/73 tests; new `tests/template-seam.test.js`.
**Why the registry mattered (real bug caught):** product pages send a CAPITALISED display name (`scribble.html` → `template: 'Scribble'`, `wander.html` → `'Wander'`), stored verbatim as `templateName`. The original literal `=== 'wander'` would have silently rendered every Wander order as Scribble. Lowercase-normalised registry lookup fixes it. **Rule saved to memory `project_template_seam.md`:** registry key = product-page `template:` value lowercased; add a template = one entry per the 3 surfaces.
**Verified (Scribble no-regression gate):** staff engine loads AEV-20 fine; customer-preview renders identically. PDF leg deferred (browser surfaces were the real risk).
**Watch out for:** Local-dev `npx serve` clean-URLs quirk — `customer-preview.html?token=X` 301-redirects to `customer-preview` and **drops the query string** → "No preview link" with no console error. Locally, open the customer page WITHOUT `.html`: `…/customer-preview?token=X`. Not a code bug; live (Cloudflare Pages) keeps the query.

### ▶ NEXT SESSION
1. **chunk-022 — Travel map functional page** (next dependency before finishing Wander). Design locked S25 (see 15:50 entry + `docs/briefs/wander-template.md`): left = regional map SVG (6 by region code), right = staff itinerary textPanel over `FP 01 Map Right.svg`, pin = `GEO PIN.png` centre-anchored 12×23mm. Order form: country multi-select, no upload, blocks cross-region.
2. Then **finish chunk-010 (Wander)**: register Cormorant Garamond (@font-face + font picker + export-pdf FONT_MAP), wire Wander product page, end-to-end test a real Wander order. When testing the Wander product flow, confirm `wander.html` sends `template: 'Wander'` (→ registry key `wander` ✓).
3. Optional: run the deferred Scribble **PDF** leg (`npm run pdf -- <scribble-order>`) to fully close the chunk-020 gate.
