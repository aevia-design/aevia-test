# Session 193 — 2026-09-24

**Four cards closed and verified live by the owner (#129, #102, #138, #137), plus three small
Heirloom/collections fixes. Two new briefed cards: #140 (AI captions), #139 (cloud costs, no brief yet).**

Commits: `07c07e9` (monogram cards + collection counts), `cdf0a7d`/`66afe73` (#129), `b093a4e`
(engine → Dashboard button), `41360f1`/`f49eb83` (#138 brief), `905dc22` (#138 merge), `45e1be4`
+ `.dockerignore` fixes (renderer image), `1271645` (#137), `ebc64cc` (#140 brief), plus this handover.

---

## What shipped
- **Heirloom product page (EN+DE):** Beige monogram cards were cropped off-centre (its artwork's
  monogram sits ~6mm left). Per-colourway `MONO_X` crop; LEARNINGS says to update it when
  Blue/Brown/Green get Beige's coordinates. Cards renamed **Bond / Harmony / Devotion**, and the
  descriptions drop the bold lead word. `.mono-desc` reserves 2 lines, not 3.
- **Collections pages** count their own `.template-card`s (Adventures said 2, has 3).
- **#129** customer preview records caption line breaks; approval promotes `customerCaptionLines`
  instead of nulling `staffBookCaptionLines`. Its hidden-spread measurement trap is handled by
  `collectCaptionLinesFully()`. **Verified by owner on AEV-070 (Wander).**
- **#102** closed by the same test: the full-bleed reposition printed exactly. **#67 (bold in PDF) was NOT tested.**
- **Staff engine** nav CTA → Dashboard.
- **#138** PDF generation control: shared status in every tab, one render per order (409 in a
  transaction, 20-min stale guard), confirm on fresh Generate, cancel × (cooperative, stops the
  render loop within ~1.5s, uploads nothing). Renderer now `--concurrency 1` (revision 00043).
  **Verified live by owner.**
- **#137** Dorottya's portrait: folder and file renamed to the unaccented path the pages already used.

## Decisions
- #129 went before #99. **#99 must carry `customerCaptionLines` along with captions** whatever it decides.
- Cancel is cooperative, not a hard kill (owner agreed).
- Captions (#140): Step 1 = word-overuse control in code + humour for Kids/Adventures on the current
  model; Step 2 (cheap-tier model trial, ≤ €0.30/book) only if the owner asks. **About €0.50 per book is too much.**
- Every brief goes through `/creating-briefs` (saved to memory).

## Watch out for
- **Renderer image builds from the WORKING FOLDER** (untracked files included). "Container import
  failed" came from untracked website folders; see LEARNINGS S193. New renderer `require`s need a
  Dockerfile `COPY` **and** a `.dockerignore` exception.
- `npm test` inside an agent worktree finds 0 tests (jest ignores `.claude/`). Agents need
  `--testPathIgnorePatterns=node_modules`. No card yet.
- Untracked, not mine: `assets/about us photos/`, `assets/packaging/`, the vector logotype, `qa/debug-*` scripts.
