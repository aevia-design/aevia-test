# Session 27 — 2026-06-04

### 10:50 — QA testing knowledge captured before template-2 pivot
**Done:** Wrote `qa/README.md` — indexes the 7 browser-test scripts, captures reusable techniques (Stripe Karte-radio-before-fill, wait-for-data-not-timer, status-aware chains, headless-so-human-does-creative-edits handoff) + gotchas; flags which scripts are Scribble-specific (minting) vs template-agnostic (downstream/probes). Pointed CLAUDE.md at it; added memory `project_qa_scripts.md`.
**Why:** The know-how was scattered only across STATUS.md + session logs 22–26 and would be lost as those scroll out of context during Wander work. Scripts themselves were already committed — only the knowledge was at risk.

### 11:30 — chunk-022 map render: helper + smoke test (NOT yet in engine)
**Done:** Built `assets/js/map-render.js` (shared helper: `pickRegion`, `resolveMapSvg`, `placeMapPins`), `tests/map-render.test.js` (7/7 pass), and standalone harness `pages/staff/map-smoke.html`. Smoke test verified in browser: pick EU countries + type itinerary → correct region map SVG, 6 pins placed at country coords, itinerary panel in Cormorant Light, zero console errors.
**Decided:** Extract the new map logic into ONE shared helper that the smoke page, staff engine, and customer-preview all call — rather than writing it inline in the engine. Same pattern as `book-completeness.js`.
**Why:** A standalone smoke harness lets us prove the map render in isolation before touching the 2,700-line engine; the shared helper means we debug the map once, not twice, and avoids staff/customer parity drift.
**Watch out for:** Pins are CENTRE-anchored per the Wander data contract (`pin.anchor:'center'`, coords are pin centre) — not tip-anchored. If pins later look wrong, that's a one-line change in `placeMapPins`, but the data convention must change too.

### 11:45 — BLOCKED on Kseniia: map SVGs need re-export
**Decided:** Pause chunk-022 engine wiring. User reviewed the smoke render and found the region map SVGs themselves are "a bit weird" — Kseniia needs to fix and re-upload them.
**Watch out for:** Don't wire the map page into the engine until the corrected SVGs land — the pin coordinates in `wander-data.js` (`mapCoordinates`, 183 countries) were measured against the CURRENT maps, so new SVGs likely require re-deriving the coordinates from a fresh `Map_Coordinates_upd.csv`. The smoke page (`map-smoke.html`) is the tool to re-verify placement once new assets arrive.
**Next when resumed:** wire helper into `template-engine.html` (mapCanvas branch + staff country picker) → mirror into `customer-preview.html`. PDF + customer QA still deferred until Wander fully built.
