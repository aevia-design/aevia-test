# Session 76 — 2026-06-23

**Planning-only session. No code shipped.** Designed the **artist-collaborations** feature end to end and produced a build-ready brief. Ran the full prep chain: `/ideating` → `/understanding-the-ask` → `/conducting-research` → `/creating-briefs` → `/stop-slop`.

## What we decided
The feature has three parts:
1. **"In collaboration with Kevin Lucbert"** credit on the Wander product page + collections card — understated single line near the title (existing small-caps/gold label style, NOT an e-commerce badge), linking to the artist's section on a new Our Artists page (on-site, not straight to Instagram).
2. **`pages/our-artists.html`** — a *growing roster* (not Kevin-centric): manifesto intro → artist profiles (Kevin first) → a real "collaborate with us" form (Name · Email · work link · optional note). Form submits via a new callable **`submitArtistApplication`** that emails **xenia@aevia.at** (mirrors the nodemailer pattern in `functions/index.js` ~line 765; recipient hardcoded for now, "fix later"). Portfolio is a **link, not an upload** (no infra cost).
3. **`docs/templates.md`** — new roster marking which templates are artist collaborations (Wander = first). No such file existed before; it becomes the human-readable source of truth.

Plus: **permanent footer link** to Our Artists (under "Company") on every page.

## Key decisions (Evgeny, S76)
- Label wording = "In collaboration with" (it's the back-of-book copy; on-book = on-site, identical).
- Compensation line = **"compensated fairly"** (NOT royalty-specific — royalty is one model among several).
- Bio taken verbatim (facts) from kevinlucbert.com/contact-bio, then stop-slop'd for tone. Final, not placeholder.
- Page is a roster that grows; markup must make adding the next artist a copy-paste block.

## Research takeaways (folded into brief)
Credit visible + identically worded at every touchpoint; understated line reads premium, loud badge reads cheap; place at provenance/title area; an opinionated manifesto is a feature; publicly stating fair pay is a differentiator + recruiting signal; minimise form fields, use a link not an upload, show an immediate confirmation state. Sources in the conducting-research output.

## Asset housekeeping
Normalised the portrait Evgeny dropped in: `assets/artists/Kevin Lucbert/KevinLucbert-portrait-web2.jpg` → **`assets/artists/kevin-lucbert/kevin-lucbert-portrait.jpg`** (hyphenated lowercase, no spaces). Untracked, not yet committed.

## Decided NOT to do
- `/decomposing-work` — overkill. Three small parts with an obvious backend-first order; the brief already reads as a build plan. (Default-to-simplicity.)

## ▶ NEXT SESSION (Session 77) — BUILD the feature
Read `docs/briefs/artist-collaborations.md` first (copy is final + embedded in the brief). **Backend-first ordering:**
1. **Write `submitArtistApplication`** in `functions/index.js` (callable; validate name/email/≥1 link; nodemailer → xenia@aevia.at; return result the page can render success/failure from — no silent "lie", per S40 rule). Evgeny deploys: `firebase deploy --only functions:submitArtistApplication`.
2. **Build `pages/our-artists.html`** — nav/footer copied from an existing page; manifesto + Kevin profile (portrait + bio + Instagram link) + the form wired to the function; link `assets/css/mobile.css`; single-column reflow at 375px.
3. **Wander label** on `pages/wander.html` (near title) + the card in `pages/collections.html` (~line 302), linking to `our-artists.html#kevin-lucbert`.
4. **Footer link** to Our Artists under "Company" — on EVERY page that carries the footer (duplicated per page, like the nav).
5. **`docs/templates.md`** roster (Wander = first collaboration; artist + relationship).
6. Run `/design-review` (desktop + mobile, console clean). Merge to `main` only after the function is deployed, or the live form errors.

## ⚠ Watch-outs
- **Backend-first or the live form breaks** (carried discipline S40/S64). Function must be deployed before the page reaches Cloudflare.
- **Footer is duplicated per page** — the Our Artists link must land on all footer-bearing pages, not just the new one.
- Two placeholder-but-now-final copy items are locked: bio facts (from his site) + "compensated fairly" wording. Don't reopen without reason.
- `.claude/settings.local.json` left out of commits as usual; stray `sessions/2026-06-23-s71.md` left alone (pre-existing).
- New untracked: `assets/artists/`, `docs/briefs/artist-collaborations.md` — commit with the build (or now, as planning artefacts).
