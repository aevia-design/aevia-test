### — Cover caption colour wired end-to-end from CSV
**Done:** Added `Captions_1_fontColor` / `Captions_2_fontColor` columns to `Scribble_Template_Sizing_Cover.csv`. `parseCoverCsv` reads them; `serializeCover` emits `color: '#...'` on each cover caption. `drawCoverCaptions` now resolves and applies the colour via a local `resolveCapColor()` (hex only — cover doesn't use named colours).
**Watch out for:** Front captions previously rendered as black because `pg.drawText` was called without a `color` param. Now both rotated (spine) and non-rotated (front) captions get the colour. If a hex isn't 6 chars or is missing, falls back to near-black `CAPTION_COLOR`.

### — Spine caption rotation direction and positioning fixed
**Done:** Two bugs: (1) `angle: capDef.rotate || 270` produced 270° CCW in pdf-lib = text reading DOWNWARD. CSS `rotate(270deg)` CW equals 90° CCW in pdf-lib. Fix: hard-code `angle: 90`. (2) Positioning math had x/y roles swapped — `xMm` (horizontal spine center) was used as the y-axis target and vice versa. After 90° CCW rotation, the drawText `x` parameter controls horizontal position on spine; `y` controls vertical position along spine length. Rewrote spine branch with correct math.
**Watch out for:** pdf-lib uses standard math convention (CCW = positive); CSS uses CW = positive. Any future rotation work must convert: `pdf_angle = 360 - css_angle`. After 90° CCW rotation, character body extends LEFTWARD from drawing baseline (x) by approximately cap height, and UPWARD from drawing baseline (y) by text width.

### — Spine horizontal centering refined with font ascender height
**Done:** First fix used `sizePt / 2` as the rotation centering offset — but cap height of typical fonts is only ~70% of `sizePt`, so visual centre ended up biased to one side. Switched to `font.heightAtSize(sizePt, { descender: false }) / 2` (ascender height) for accurate centering on uppercase spine text.
**Watch out for:** This assumes uppercase-heavy spine text (ANN, 2026). For mixed case with descenders the offset is still slightly biased; not worth solving now.

### — Engine caption font size was 26% too big (CSS pt at 96dpi)
**Done:** Root cause: `captionEl.style.fontSize = sizePt + 'pt'` — browsers resolve CSS `pt` at 96dpi (1pt = 1.333px), but the engine canvas is ~76dpi (SCALE=3 px/mm). Result: a "14pt" caption rendered as 18.67px on screen instead of the correct 14.82px for print-equivalent. Captions appeared larger than the PDF and wrapped differently. Fix: convert pt → canvas px explicitly using `sizePt * SCALE * 25.4 / 72`. Applied in both `renderSpread` (line ~2033) and `applyStyleToSpreadCapEl` (line ~1623, the toolbar live-update path).
**Watch out for:** Do NOT use CSS `pt` units anywhere that needs to match print scale. The FunnyWords panel already uses `sizePt * SCALE` (px) by deliberate convention — leave that path alone. If a future template uses a different SCALE, the formula auto-adjusts.

### — Medium style (weight 500) was never reaching the PDF
**Done:** `drawCoverCaptions` and `drawCaptions` both resolved style as `>=700 → bold`, `>=600 → semibold`, then dropped straight to `italic/regular`. Weight 500 (Medium pill in engine) fell through to 'regular'. `NT Somic_medium` was in FONT_FILE_MAP but never reached. Added the `>=500 → 'medium'` branch in both functions.
**Watch out for:** EB Garamond has no `_medium` variant — it's a variable font and would fall back to `_regular` via `lookupFont`'s fallback. Acceptable for now.

### — `upper-right`/`lower-right` captions were overflowing the page; `above`/`below` got `wrapText` too
**Done:** Root cause: pdf-lib does NOT auto-wrap. Right-margin captions had no width constraint — they drew the full text string and bled off the page edge. Added `wrapText(font, text, sizePt, maxWidthPt, charSpacing)` helper that word-wraps to fit, with character-level fallback for words wider than the line. Applied to all three branches. The width formula matches the engine's: gap from photo = `capDef.offset` (mm, NOT a hardcoded 2mm), right padding = 2mm to mirror the engine's 6px gutter.
**Watch out for:** Initial fix used a fixed 2mm gap which gave the PDF more room than the engine and made wrap points differ (PDF fit one extra word per line on FP3 left V upper-right). Use `capDef.offset` for the gap so engine and PDF produce identical line breaks.

### — Cover caption sizes increased to 16pt via CSV
**Decided:** User edited `Scribble_sizing_full.csv` to 16pt across most spread captions. Reminder: editing the CSV does NOT update `scribble-data.js` — must run `node csv-to-template.js` then hard-reload the engine. Documented this in the engine workflow.

---

_Note: the four entries below were originally written to `sessions/2026-05-27.md` by a previous Claude session today (commit 8a3e395 at 13:12 on 2026-05-26). The date in the filename was a hallucination — the work landed today. Merged here on 2026-05-26 ~15:30._

### — Plan 12-02: fpSelections + photoCount saved to Firestore
**Done:** Added `fpSelections` (array of active FP slugs, uppercased e.g. `['FP1','FP3']`) and `photoCount` (main pool file count) to the order form payload (`pages/order.html` line ~1298) and Firestore schema (`functions/upload.js` line ~82, ~254). Both fields now saved alongside existing `fpTexts`.
**Why:** Needed for Plan 11-03 (engine "Load order" flow) to know which FP spreads to render and how many photos were submitted.
**Watch out for:** Slugs are uppercased in the payload (`FP1` not `fp1`). The earlier plan 11-02 was drafted assuming lowercase — when 11-03 / `getOrderAssets` is implemented it must match the actual stored case.

### — orderFormPhoto/orderFormMeta wiped by CSV script
**Done:** Restored `orderFormPhoto`/`orderFormMeta` to all 5 FP spreads in scribble-data.js. Created `assets/Template_Scribble/scribble-orderform.json` as a sidecar file. Updated `csv-to-template.js` to read and inject the sidecar when serializing FP spreads.
**Why:** The CSV regeneration script had no knowledge of these fields — every `node csv-to-template.js` run silently wiped them, breaking FP upload zones and text fields in the order form.
**Watch out for:** If adding a new FP spread, also add its entry to `scribble-orderform.json` — the script reads from there, not from scribble-data.js. Fields in scribble-data.js are overwritten on every CSV run.

### — Order form photo grid: sequential queue + incremental DOM
**Done:** Rewrote `handleMainFiles` as a sequential async queue (`drainUploadQueue` / `processAndAddFile` in `pages/order.html` ~line 938-948). Replaced full-rebuild `renderPhotoGrid` pattern with `appendPhotoThumb` (line ~998, appends one thumb) + `refreshThumbBadge` (line ~1005, patches badge only). Full rebuild (`rebuildPhotoGrid`, line ~1077) now only runs on remove.
**Decided:** Sequential processing, not parallel — matches template engine pattern exactly.
**Why:** Old approach fired ~30 parallel async res-checkers each calling `grid.innerHTML=''` + full rebuild. For 30 photos: ~465 img decodes cascading. Browser flushed all deferred paint on alt-tab return → 5-10s stall. Template engine avoids this by processing one file at a time and appending incrementally.
**Watch out for:** `_objUrl` is now created synchronously (lines 961, 968 — before `mainFiles.push`) for JPEG/PNG, so the thumb always shows the image on first paint. Do not move URL creation back inside `renderPhotoGrid` — that was the original cause of the stall.

### — updateStep2Bar was never defined
**Done:** Added `updateStep2Bar()` function (`pages/order.html` line ~538) — recalculates `calcPhotoTarget` for remaining active FPs and refreshes the photo count callout + status bar.
**Why:** `cancelAddon()` called `updateStep2Bar()` but the function was never defined, so removing an FP addon silently left the required photo count stale.

### — Mobile responsiveness not yet done (TO-DO #47)
**Decided:** Added as TO-DO #47 (High). Not tackled in the order-form session.
**Why:** Discovered during testing — `pages/home.html` and `pages/order.html` both unoptimised on mobile. Template engine is staff-only so skipped. Priority order: home.html first (marketing/first impression), then order.html.

---

### — Plan numbering reconciled; missing 11-04 plan written
**Done:** Created `.planning/phases/11-order-connection/11-04-PLAN.md` (PDF export wired to GCS). Updated ROADMAP.md Phase 11 to show all 4 plans with correct statuses. Fixed whats-next.md which used invented "Plan 12-03 / 12-04" labels that did not match the real ROADMAP numbering.
**Why:** whats-next.md had been referencing plans that did not exist as files. The actual ROADMAP uses Phase 11 (Order Connection), not Phase 12, for the load-order + PDF-to-GCS work.
**Watch out for:** Phase 12 in the ROADMAP is the (already done) standalone PDF export. Do not confuse it with the deferred Phase 11 work that connects PDF to the order pipeline.

### — Phase 11 deferred until engine geometry settles
**Decided:** Park all of Phase 11 (Plans 11-02, 11-03, 11-04 — order integration). Local photo upload stays the dev loop.
**Why:** Phases 13 (bleed SVGs) and 14 (caption CSV coordinates) will both change engine + PDF geometry. Wiring order flow now means redoing it after geometry settles. Local upload is faster for stress-testing the engine while iterating on SVGs / caption coords.
**Watch out for:** When Phase 11 resumes, run 11-02 → 11-03 → 11-04 in order. Plan 11-04 specifically depends on trustworthy PDF output, so do not start it until at least one print test passes.

### — TO-DOs #48 and #49 added (urgent template engine work)
**Done:** #48 — re-upload SVGs with 3mm bleed, adjust CSV coords, implement Plan 13-01 (engine clips bleed, PDF places SVG at origin). #49 — add caption box x/y/w/h columns to spreads CSV like cover CSV does, regenerate scribble-data.js, update engine + export-pdf.js to use explicit box dims. New Phase 14 added to ROADMAP for #49.
**Why:** Kseniia flagged both. (a) Print machines cut 1–3mm imprecisely — graphics need to extend into bleed to avoid white edges. (b) Current spread captions sometimes land too close to template borders in the PDF because positions are inferred from slot + offset, not explicit. Cover CSV already uses explicit caption coordinates and works correctly.
**Watch out for:** #48 needs Kseniia's new SVGs before starting. #49 has no blocker but needs a Phase 14 plan file written before implementation begins.

### — Session log merge: 27.md absorbed into 26.md
**Done:** Discovered `sessions/2026-05-27.md` had been written today by a previous Claude session — the date in the filename was a hallucination (real OS date is 2026-05-26, confirmed via git author-date on commit 8a3e395). Verified every code claim in 27.md against actual code (all real: fpSelections, photoCount, sidecar JSON, sequential photo queue, updateStep2Bar). Merged 27.md content into 26.md with file-line references for each entry, deleted 27.md, fixed all "2026-05-27" date strings in ROADMAP.md, whats-next.md, TO-DOS.md, and Plan 11-02.
**Why:** Date hallucination created a phantom session file. The work itself was real and shipped today.
**Watch out for:** When checkpointing, trust the system-injected `currentDate` field — do not infer date from prior session filenames or commit message phrasing. If unsure, run `date "+%Y-%m-%d"` via Bash before writing a session file.

### — Plan 11-02 status block added; uppercase-slug landmine noted
**Done:** Added a Status section at the top of `.planning/phases/11-order-connection/11-02-PLAN.md` marking Task 1 (schema) done and Task 2 (Cloud Function) deferred.
**Watch out for:** Plan 11-02 Task 1 example code used lowercase slugs (`['fp1','fp3']`) but the actual shipped code in `pages/order.html` line ~1298 stores **uppercased** (`['FP1','FP3']`). When the `getOrderAssets` Cloud Function is eventually built, its filename-to-slug derivation must match the uppercase form (or normalize both sides). Latent case-mismatch bug if not handled.
