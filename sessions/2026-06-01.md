### 10:30 — SVG alignment icons, toolbar clamping
**Done:** Replaced L/C/R text pills with inline SVG line icons in both engines. Fixed caption toolbar overflowing right viewport edge — now clamps to `window.innerWidth - toolbarWidth - 8px`.
**Watch out for:** Toolbar width is read via `tb.offsetWidth` at show-time. If the toolbar is hidden when positioned (display:none), offsetWidth returns 0 and fallback of 340px is used. Fine for current layout but revisit if toolbar width changes significantly.

### 10:45 — chunk-004: approveOrder flow + sentSnapshot
**Done:** New `approveOrder` Cloud Function merges `customer*` → `staff*` on approval (null-safe), sets `status: approved`, appends to `statusHistory`. `generatePreviewLink` in dashboard now also writes `sentSnapshot` — frozen copy of all 5 staff book fields at send time.
**Decided:** Overwrite `staff*` with `customer*` at approval (customer's approved version = print-ready). Alternative was keeping both — rejected because it creates ambiguity about which version to print.
**Watch out for:** `FieldValue.serverTimestamp()` cannot be nested inside `arrayUnion` — Firestore throws "Element at index 0 is not a valid array element." Use `Timestamp.now()` inside array entries, `serverTimestamp()` only for top-level fields. Fixed in `approveOrder` after hitting this in testing.

### 11:00 — Firestore rules: sentSnapshot
**Done:** Added `sentSnapshot` to the dashboard browser-write allowlist in `firestore.rules`. Was missing after chunk-004 added it to the `generatePreviewLink` write — caused "Missing or insufficient permissions" error.
**Watch out for:** Any new field written directly from the browser (not via Cloud Function) must be in the `hasOnly([...])` allowlist. Cloud Function writes bypass rules (admin SDK). Browser writes do not.

### 11:15 — Order form photo count guard
**Done:** `submitOrder()` now blocks if `mainFiles.length < window._photoCountTarget`. Error message is specific: "Please upload N more photo(s) (X of Y added)."

### 11:30 — Save warning: empty slots + spread template switch
**Done:** Save warning in staff engine now counts actual null slots in `bookAssignments` (not just pool-size arithmetic). Previously only warned on unplaced pool photos — missed slots that were empty because not enough photos were uploaded or because a spread template was switched mid-session.
**Watch out for:** The old `emptySlots` stat in the status bar (`regularSlots - pool.length`) is still there for display but is an estimate. The save warning uses the more accurate real-slot count. These two numbers may differ after template switches — that's expected.

### 12:00 — sentSnapshot audit trail: UI gap noted
**Decided:** sentSnapshot is data-only for now (visible in Firestore console). Visual audit view (`?view=sent` param on customer-preview) added as TO-DO #50. Performance improvement (signed URL direct as img.src) added as TO-DO #51.
**Watch out for:** The customer preview link always shows the live customer state — not the sent state. After approval the link reflects what the customer approved, not what was originally sent. sentSnapshot in Firestore is the only record of the original.
