### 10:30 — SVG alignment icons, toolbar clamping
**Done:** Replaced L/C/R text pills with inline SVG line icons in both engines. Fixed caption toolbar overflowing right viewport edge — now clamps to `window.innerWidth - toolbarWidth - 8px`.
**Watch out for:** Toolbar width is read via `tb.offsetWidth` at show-time. If the toolbar is hidden when positioned (display:none), offsetWidth returns 0 and fallback of 340px is used. Fine for current layout but revisit if toolbar width changes significantly.

### 10:45 — chunk-004: approveOrder flow + sentSnapshot
**Done:** New `approveOrder` Cloud Function merges `customer*` → `staff*` on approval (null-safe), sets `status: approved`, appends to `statusHistory`. `generatePreviewLink` in dashboard now also writes `sentSnapshot` — frozen copy of all 5 staff book fields at send time.
**Decided:** Overwrite `staff*` with `customer*` at approval (customer's approved version = print-ready). Alternative was keeping both — rejected because it creates ambiguity about which version to print.
**Watch out for:** `FieldValue.serverTimestamp()` cannot be nested inside `arrayUnion` — Firestore throws "Element at index 0 is not a valid array element." Use `Timestamp.now()` inside array entries, `serverTimestamp()` only for top-level fields. Fixed in `approveOrder` after hitting this in testing.

### 11:00 — Firestore rules: sentSnapshot
**Done:** Added `sentSnapshot` to the dashboard browser-write allowlist in `firestore.rules`. Was missing after chunk-004 added it to the `generatePreviewLink` write — caused "Missing or insufficient permissions" error.
**Watch out for:** Any new field written directly from the browser (not via Cloud Function) must be in the `hasOnly([...])` allowlist. Cloud Function writes bypass rules (admin SDK). Browser writes do not.

### 11:15 — Order form photo count guard
**Done:** `submitOrder()` now blocks if `mainFiles.length < window._photoCountTarget`. Error message is specific: "Please upload N more photo(s) (X of Y added)."

### 11:30 — Save warning: empty slots + spread template switch
**Done:** Save warning in staff engine now counts actual null slots in `bookAssignments` (not just pool-size arithmetic). Previously only warned on unplaced pool photos — missed slots that were empty because not enough photos were uploaded or because a spread template was switched mid-session.
**Watch out for:** The old `emptySlots` stat in the status bar (`regularSlots - pool.length`) is still there for display but is an estimate. The save warning uses the more accurate real-slot count. These two numbers may differ after template switches — that's expected.

### 12:00 — sentSnapshot audit trail: UI gap noted
**Decided:** sentSnapshot is data-only for now (visible in Firestore console). Visual audit view (`?view=sent` param on customer-preview) added as TO-DO #50. Performance improvement (signed URL direct as img.src) added as TO-DO #51.
**Watch out for:** The customer preview link always shows the live customer state — not the sent state. After approval the link reflects what the customer approved, not what was originally sent. sentSnapshot in Firestore is the only record of the original.

---

# Session 12 — 2026-06-01

### chunk-006 — PDF export pulls full-res originals from GCS (commit 2d82c79)
**Done:** Added `--order <orderNumber>` mode to `scripts/export-pdf.js`. Calls `getOrder` (staff key) → fresh signed URLs to the full-res ORIGINALS (`photoManifest`), matched to `book-state.json` by filename, downloaded on demand via one cached `loadPhoto()`. Local `--photos` mode preserved. Added `scripts/package.json` (pdf-lib, fontkit, sharp) so it runs anywhere — deps were previously not installable in this checkout.
**Decided:** Script fetches URLs itself each run (not baked into book-state.json) → can re-export weeks later, no expiry problem. Photos are the uncompressed originals (verified: 4480×2987, ~14MB cover), not render-compressed versions.
**Watch out for:** Run from repo root; deps in `scripts/node_modules`. Signed URLs expire 1h (fine — fetched fresh per run).

### Staff engine restores saved book state on load (commit bc57c36)
**Done:** `loadOrderIntoEngine` was loading photos then calling `renderBook()`, which ALWAYS auto-arranges (`assignPhotosToSpreads`) — silently discarding the saved layout AND the customer's approved changes. The "load saved state?" prompt was a lie. Now stashes saved state into `window._restoreState`; `renderBook` uses it (one-shot guard) instead of auto-arranging. Mirrors the customer engine, which already did this. Also added "Export book state (JSON)" button to the Order panel (was Local-mode only).
**Watch out for:** `_restoreState` is one-shot (cleared after use) so drag/drop + sequence edits still auto-assign normally.

### ROOT-CAUSE FIX: layout saved by photo name, not pool index (commit 11fd72f)
**Symptom:** Customer preview, staff engine, and PDF all showed the same order differently (different photo sequence, different empty slots).
**Root cause (systematic debugging):** Staff engine sorts the photo pool (EXIF date first, then filename); customer engine uses raw manifest/upload order. Assignments were stored as **positional pool indices** → the same index meant different photos in each engine. Proven on AEV-020: `photo_053.jpg` (has EXIF date 2019) sorts to staff index 0 but sits at manifest index 50; customer's index 0 is `photo_003`. AEV-019 didn't expose it (dateless sequential photos → sort == manifest).
**Fix:** Translate assignments to/from photo **basename** at the save/load boundary in both engines (`assignmentsToNames` / `assignmentsToIndices`); internal state stays index-based. Match by basename — staff stores bare filenames, customer stores full GCS paths. Legacy index saves load via numeric passthrough. Export already used names, so PDF agrees too.
**Verified:** AEV-020 renders spread-for-spread identical in staff and customer despite fully divergent pool orders.
**Watch out for:** The two helpers are parallel copies in both engines — keep in sync. Never revert to index-based saves.

### Customer-preview view-only lock after approval (commits bc57c36 + ed0f835)
**Done:** `getOrder` now returns `status` (deployed to prod). On load, if `approved`/`paid`, customer-preview disables editing (captions non-editable, photos non-draggable), hides "Save changes", shows "Approved ✓" disabled + a notice. Customer can still browse. Defensive guards in `saveBookState`/approve handler.
**Decided:** View-only (not full lock-out) — warmest, lets customer re-admire their approved book, zero data risk.

### Tested by user
- **Photo-count guard:** order form correctly blocks submitting < required photos. ✓
- **PDF export:** works great. Two issues found → TO-DO #54 (birthday spread left-page font wrong in PDF) and #55 (photo crop control, esp. heart-mask cropping faces). Flagged for next session.

### Local dev note
`npx serve` strips the `?token=` query via a clean-URL 301 redirect — broke customer-preview locally. Use `npx http-server . -p 8085 -c-1` instead (serves `.html` directly, keeps query string). Customer token works on localhost (same `getOrder` backend); just swap the domain.

### Session 13 — TO-DO #54: birthday panel PDF size
**Done:** PDF text panels now print at the engine's on-screen size. `export-pdf.js` multiplies the regular (non-funnyWords) panel sizePt by `PANEL_PT_SCALE = (96/25.4)/3 ≈ 1.2598`. Verified by user: PDF works well.
**Decided:** Compensate in the PDF only — leave both engines untouched (the bigger birthday look is wanted). Rejected: (a) shrinking the engine to SCALE-based px (user prefers the bigger look); (b) bumping CSV sizePt (can't work — same field drives engine @96dpi and PDF @72dpi, so any value keeps a fixed 1.26× gap).
**Why:** Not a font-family bug as TO-DO #54 assumed. Both use NT Somic Medium correctly. The engine renders panels in raw CSS pt @96dpi (~1.26× bigger on the 3px/mm = 76.2dpi canvas); the PDF read sizePt as true 72dpi pt, printing ~1.26× smaller.
**Watch out for:** PANEL_PT_SCALE hardcodes SCALE=3 px/mm. If the engine SCALE ever changes, update the constant. Only regular text panels are affected — funnyWords (mm-based) and slot captions already matched. The S8 "do not normalise" watch-out still holds for the engines; the normalisation lives in the PDF.

### Session 15 — chunk-005: Stripe Checkout payment flow
**Done:** Full end-to-end payment flow integrated. `createCheckoutSession` Cloud Function (customer-token or staff-key auth) creates a Stripe Checkout Session for approved orders and returns the redirect URL. `stripeWebhook` Cloud Function verifies Stripe signature, sets order status → `paid`, appends to `statusHistory`, sends staff notification email. Idempotency guard added (skips if already `paid`). `customer-preview.html`: "Pay now" button shown for approved orders; `?payment=success` on return triggers a toast + bottom-bar confirmation. `stripe ^14.0.0` added to `functions/package.json`. Verified end-to-end with test card `4242 4242 4242 4242`. All committed and deployed.
**Decided:** Prebuilt Checkout (hosted redirect), not embedded components or Payment Links. Redirect is fine for a single high-stakes payment step; saves 2–3 sessions of build complexity. One generic "Aevia Photo Book" product in Stripe for now — per-template SKUs deferred until multiple templates exist.
**Watch out for:** Price set server-side via `STRIPE_PRICE_ID` env var — never from browser. Three Stripe env vars in `functions/.env`: `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_ID` — swap all three for `sk_live_` equivalents when going live. Test card `4242...` simulates a US card (3% + €0.25 fee); use `4000002500000003` (Austrian Visa) for accurate 1.5% + €0.25 fee simulation. Webhook endpoint in Stripe dashboard must point to `https://europe-west1-aevia-uploads.cloudfunctions.net/stripeWebhook` with event `checkout.session.completed`.

### Session 14 — Heart-page photo crop control (TO-DO #55)
**Done:** Staff can drag the heart-mask photo to reposition it inside the heart so it never crops a face. New `heartCrop` state = per-photo `object-position` % (`{x,y}`, default 50/50 = old centred behaviour), keyed by photo name. Drag handler (`attachHeartDrag`) + render-apply in `template-engine.html`; read-only apply in `customer-preview.html`; PDF replicates in `export-pdf.js`; persisted via local export, cloud save/load (`staffHeartCrop` in `functions/index.js`, both `saveStaffState` and `getOrder`).
**Decided:** Manual drag-to-reposition, heart slot ONLY (not all slots). Rejected auto face-detect (silent-failure risk for a "never crop heads" rule) and all-slots scope (other slots match photo orientation, so centre-crop is fine — no real problem yet).
**Why:** Heart mask pinches at top/bottom, so a centre-cropped vertical photo loses the face. Other pages + full-bleed tested fine.
**Watch out for:** Parity rests on ONE number — engine sets `object-position: x% y%`; PDF computes `window-left = (scaledW − CONTENT_PX) × x/100` (exact inverse of CSS). Don't change one side's math without the other. Heart slot is `pool:'special'` (non-draggable normally) — `attachHeartDrag` is a dedicated pointer handler, separate from slot-swap drag; img `draggable=false` to kill native drag-ghost. Drag only moves the overflowing axis (vertical photo → up/down; horizontal → left/right; square → no movement). NOT yet tested in customer-preview view (read-only mirror, expected to match). EXIF-orientation mismatch between processed pool photos and GCS originals (`--order` mode) is a pre-existing whole-pipeline concern, not this feature.

---

# Session 16 — 2026-06-01

### Planning session — PDF pipeline design (chunk-007)
**Decided:** chunk-007 restructured into three parts: (1) "Export" button in staff engine writes `book-state.json` to GCS; (2) local script renders both PDFs and auto-uploads to GCS; (3) dashboard shows download links via signed URLs. ROADMAP.md updated with detailed acceptance criteria.
**Render job decision:** Option A (enhanced local script) for now — only Evgeny needs to generate PDFs during development and early production. Option B (GitHub Actions cloud render) added to roadmap as the upgrade path for when Kseniia needs to trigger renders remotely. Decision is reversible — the script itself won't change, only the trigger mechanism.
**PDF retention policy decided:** Preview PDF deleted when order moves to `approved` (internal QA only, served its purpose). Print PDF kept 90 days after `sent_to_print` then auto-deleted. At current volumes GCS storage cost is negligible (~€0.008/PDF/month).
**Deferred:** #55 customer-preview heart crop verification — will test with a real new order. Stripe live mode — when real website is deployed.
**Watch out for:** chunk-007 Part 2 generates two named PDFs (`preview.pdf` and `print.pdf`) — not random names. Preview PDF is internal only, never shown to customers.
