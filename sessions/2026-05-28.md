### 10:00 — Customer journey doc updated

**Done:** `context/customer-journey-v1.md` rewritten to reflect current build state and revised north star journey (11 steps).
**Decided:** PDF tool is print-production only (post-approval + payment), not part of customer preview flow. Customer preview = limited engine (9b, Must Have) + page-flip mockup (9a, Should Have).
**Watch out for:** Journey doc is now accurate as of 2026-05-28. Future sessions should update it when steps are built or decisions change — it's the canonical flow reference.

### 11:00 — PRD created

**Done:** `PRD.md` written from scratch covering full end-to-end pipeline. All 11 journey steps formalised into MoSCoW requirements with testable acceptance criteria.
**Decided:** MVP covers all phases (not phased delivery) — first 20–30 orders are friendly testers who need the full cycle. 9 templates is a core MVP deliverable, not P2.
**Decided:** Customer preview interface is desktop-only. Mobile shows a friendly gate ("open on a desktop"). No degraded mobile UI built.
**Decided:** Payment link presented immediately on customer approval — no manual staff step between approve and pay.
**Watch out for:** German language is P1 (not Won't Have). Stripe account not yet set up — blocks payment step build.

### 11:30 — ARCHITECTURE.md + ADRs created

**Done:** `ARCHITECTURE.md` written covering system diagram, full codemap, key patterns (template-as-data-file, three-mode engine, Firebase backend), data flow for all 6 steps, 7 invariants, cross-cutting concerns, and dependencies. `docs/decisions/0001` and `docs/decisions/0002` created as ADRs.
**Decided:** ADR-0001 — Staff engine public hosting: Cloudflare Access (Zero Trust, OTP to allowed emails). Rejected HTTP Basic Auth (shared credential, no expiry, no audit trail, no per-user revocation — not appropriate given customer photos/data at stake).
**Decided:** ADR-0002 — Customer preview token: UUID stored in Firestore as `previewToken` per order. Rejected HMAC (cannot revoke individual tokens without a blocklist; no performance benefit at MVP scale).
**Watch out for:** Staff engine currently local only — Cloudflare Access setup needed before Xenia can use it remotely. Preview token implementation needed before `customer-preview.html` build can begin.

### 12:00 — Architecture clarifications

**Decided:** PDF export script target state is shared (both founders), not permanently local. Options: each installs Node locally (near-term) or Cloud Run job triggered from dashboard (long-term). Open question logged.
**Decided:** Customer mode of the engine may need a distinct visual skin, not just feature-flag toggling — current UI is staff-optimised. Scope TBD.
**Watch out for:** `assets/` will grow to 9 template folders, each with its own CSV pair, data file, SVGs, and potentially fonts. The codemap pattern (`Template_[Name]/`) is documented in ARCHITECTURE.md.

### 14:00 — CLAUDE.md rewritten lean

**Done:** `CLAUDE.md` reduced from 191 lines to 52. All absorbed content (Firebase function signatures, template engine detail, pipeline phases, folder tree) removed — those now live authoritatively in PRD.md, ARCHITECTURE.md, and STATUS.md.
**Decided:** CLAUDE.md is a pointer file only. Rule: if the information lives or should live in another file, CLAUDE.md points to it, never absorbs it.
**Watch out for:** Future sessions — do not add detail back into CLAUDE.md. If something feels missing, add it to the right dedicated file and add a pointer.

### 14:10 — whats-next.md retired

**Done:** `whats-next.md` (project file) deleted. `commands/whats-next.md` skill deleted. `checkpoint` and `prime` skills updated to remove all references to it.
**Decided:** Minimal two-file logging model: `sessions/YYYY-MM-DD.md` for decisions (checkpoint writes here), `STATUS.md` for current build state + next steps. Nothing else.
**Why:** `whats-next.md` was duplicating STATUS.md and had grown to 450 lines of stale content. The split of roles (log vs current state) is cleaner and easier to maintain solo.
**Watch out for:** The `whats-next` slash command no longer exists. Use `/checkpoint` mid-session and update STATUS.md at session end.

### 15:00 — Session 3: design system + roadmap enrichment

**Done:** `.interface-design/system.md` created — full UX spec for the customer preview engine. Two-mode layout (Edit mode = stripped staff engine; Preview mode = spread-by-spread read-only proof view). Token set inherited from `design-principles.md`. Mobile gate spec, states table, approve flow copy, explicit list of what's removed vs staff engine.
**Decided:** Customer engine is a new page (`customer-preview.html`), not a mode toggle on the staff engine. The two modes are: Edit (functional, based on staff engine minus staff controls) and Preview (the "atelier proof" feeling — chunk-018 page-flip viewer).
**Decided:** Photo swap in customer mode uses the same thumbnail sidebar drag-drop mechanic as the staff engine — no need to invent a new interaction.
**Decided:** Customer photo replacement (uploading a new photo to replace a slot) is Could Have, not MVP. Added to PRD.md with acceptance criteria. Scope: strictly 1-for-1 per slot.
**Watch out for:** The Preview mode (beautiful page-flip) is chunk-018 — do not conflate it with the Edit mode being built in chunk-003.

**Done:** ROADMAP.md chunks 001–004 enriched with acceptance criteria, pattern references, contextual notes. All four chunks are now self-contained build specs.
**Done:** `.planning/ROADMAP.md` marked ARCHIVED; Phase 13 statuses corrected to done.
**Watch out for:** chunk-004 (Approve flow) should build `approveOrder` to return a Stripe Payment Link URL from config — don't block on Stripe account creation. Use a placeholder URL until chunk-005 is ready.

### 14:30 — ROADMAP.md created (full pipeline)

**Done:** New `ROADMAP.md` at project root replacing `.planning/ROADMAP.md`. 19 chunks covering all remaining MVP work: customer preview pipeline (chunks 1–4), payment + print handoff (5–8), Cloudflare Access + 8 templates (9–17), P1 enhancements (18–19).
**Decided:** Roadmap scope expanded from "template engine only" to full 11-step customer pipeline, aligned with PRD.md and ARCHITECTURE.md.
**Decided:** 8 remaining templates are chunks 010–017, all independent and parallel — done in whatever order artwork arrives from Kseniia.
**Watch out for:** Template chunks (010–017) are placeholders — names and asset paths will fill in as each template is assigned. Don't start any template chunk without the CSV and SVG files from Kseniia first.
