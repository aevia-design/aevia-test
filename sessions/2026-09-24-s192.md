# Session 192 — 2026-09-23/24

**A backlog sweep: 12 cards closed, most built by developer agents and verified by Claude on the
live rig.** Clean public URLs are live, the German travel-map form speaks German, and the product
pages stopped promising uploads that do not exist.

Commits: `e4e7cdc` (upload-failed email), `b3c05d1` (#111), `46028cb` (#116), `b994b27` (#84),
`d41293d`/`e09eb9f` (#82 spike, added then removed), `16ef895` + `94f5843` (#82), `9d4c788` +
`53901e0` (#133), `c558f15` (#78), plus this handover.

---

### ✅ #89 closed — and the upload-failed email rewritten
Both live checks green: AEV-101 flipped to `upload_failed` and emailed; Retry on AEV-102 went
through. The owner pointed out the email's "reply to us" was meaningless (we have no photos), so it
now says "place the order again" with a button to the template's product page, `/de/<slug>` for
German orders. Emails are English-only for now; #113 carries the German version.

### ✅ #111 — a slow decode is flagged, not guessed
The agent's header-dimension fallback was **rejected after testing on 291 photos**: 288 had no
header size and upscaled files keep a stale one (1537×1023 on a 2704×1800 file). Shipped the
simple fix instead: wait 60s, then badge "Check orientation". Badge appearance not seen (staff
login layout); logic verified in the browser.

### ✅ #116 — upload-stall instrumentation
`progressEvents`, `firstProgressMs`, `lastProgressMs` per failed attempt, stored verbatim by
`reportUploadFailure` (no whitelist, no deploy). The agent added `sharp` to `package.json` while
setting up its worktree — dropped before merge.

### ✅ #82 — clean public URLs (ADR-0010)
Researched (`work/url-structure/`, 14 sources), decided by /solutioning, briefed, spiked, built.
- **The agent's spike said "option C fails"** — it tested with `wrangler pages dev`. A throwaway
  rule on the real rig proved otherwise: `/x /pages/x 200` works; only the `.html` destination
  bounces. **wrangler is not a faithful test of `_redirects`** (LEARNINGS).
- Live crawl: 27 public pages at their clean address, 27 unique internal links all 200, none
  landing on an old `/pages/` address. Found a pre-existing missing portrait → #137.

### ✅ #133 — German country names
CSV column + `mapCoordinates[name].de` via new `scripts/sync-country-de-labels.mjs`; names per
Auswärtiges Amt. Display-only: the order still stores "Austria". Verified live on both forms.

### ✅ #78 — build nothing, fix the copy
Research (`work/photo-upload-options/research_v1.md`): no mainstream competitor offers
Drive/Dropbox on a web form. **Claude corrected the synthesis**: its "60-photo Google Picker cap"
was Chatbooks' own product limit. The sweep also caught a second false promise ("you'll receive an
upload link") on 6 pages.

### Closed without code
#92 (AEV-102 proves the confirm path), #95 (spine colours match the artwork), #98 (only AEV-043, a
test order), #101 (shipped S178/S180). #90 dropped by the owner.

### Parked
**#99** — owner direction: no staff edits after the preview is sent; unlock only on a reported
issue. Open: what re-send does to the customer's draft. Found: `approveOrder` lets a customer
approve during `issue`. Needs a brief + /solutioning.

### Watch out for
- **Agent worktrees run a partial test suite**: jest ignores `.claude/` and the PDF suites lack
  `sharp`. Always re-run `npm test` in the main checkout after merging.
- **Uncommitted files are invisible to a worktree agent** — commit the brief first (the #82 spike
  agent stopped on a missing brief).
- Two agent worktrees could not be removed (locked by a leftover process); `git worktree prune`
  later.
