### — captions_color column wired end-to-end
**Done:** Added `captions_color` CSV column → `parseCaptionStyle` reads hex or named color → serialized as `color: '#493955'` in template-data.js → `resolveColor()` in export-pdf.js handles both hex and named colors. All FP and Standard spread captions now use plum `#493955`.
**Watch out for:** `resolveColor()` falls back to near-black if the value is missing or unrecognised. `DATA.colors` lookup handles named colors — if a new named color is added to CSV, it must also exist in that map.

### — captions_alignment column wired end-to-end
**Done:** Added `captions_alignment` CSV column → `parseCaptionStyle` reads it as `align` → serialized in template-data.js → engine applies `captionEl.style.textAlign = cap.align || 'center'` → PDF `drawCaptions` uses `align` for x-position math (`left` = slot left edge, `right` = slot right edge minus text width, `center` = slot center minus half text width).
**Watch out for:** `upper-right` and `lower-right` positions are rendered in separate branches that compute their own x — the `align` field is only used in the `above`/`below` branch. Right-margin captions are always left-aligned by their x placement, not by the `align` value.

### — Style pills enabled for spread captions (Bug 4)
**Done:** Removed `if (_tbMode === 'spread') return;` early-return from `rebuildStylePills`. Pills (Regular/Bold/Italic/SemiBold) now render for all caption types including art gallery slots. `applyStyleToSpreadCapEl` now sets `fontWeight` and `fontStyle` on the DOM element so engine preview reflects selection. Pills write to `spreadCaptionStyles[si][side][slotIdx]` → read by `drawCaptions` in PDF via `scsOverrides[i]`.
**Watch out for:** B/I execCommand buttons remain — they add `<b>`/`<i>` HTML tags for inline word-level styling in the engine preview. PDF strips these via `stripHtml()`. Whole-caption style (weight/italic) must be set via the pills, not execCommand.

### — lower-right caption position corrected
**Decided:** `lower-right` = outside the photo in the right margin, bottom-aligned. NOT overlaid inside the photo.
**Why:** Earlier implementation placed caption inside the slot boundary (overlapping the photo). Correct behaviour mirrors `upper-right` (right-margin column) but anchored to the slot's bottom edge instead of top.
**Watch out for:** Engine: `capLeft = sLeft + sW + gap; capTop = sTop + sH - capH`. PDF: `x = BLEED_PT + slotRight + 2mm`, lines stack upward from slot bottom using `.slice().reverse()`.

### — upper-right missing from PDF drawCaptions (Bug 1)
**Done:** Added explicit `upper-right` case in `drawCaptions`. Previously `upper-right` had no case — fell into the `else` block and was treated as `below` (used slot bottom + offset as y-anchor). Fix: x = 2mm right of slot right edge, lines run downward from slot top.
**Watch out for:** The `if/else if/else` order in `drawCaptions` is now: `upper-right` → `lower-right` → `else (above/below)`. Do not collapse these back into a single else.

### — FP5 photos still missing after Bug 3 fix

**Done:** Found the real root cause. Engine save code always did `specialPhotos[key] = names[0]` — a plain string — for every key including FP5. Export code did `Array.isArray(artArr)` → false → null → no photo. Two fixes: (1) engine now saves FP5 as an array `["leftPhoto", "rightPhoto"]`; (2) export code has legacy string fallback (uses same photo for both pages).
**Watch out for:** Any existing `book-state.json` has `FP5: "art-1.jpg"` (string). The legacy fallback handles it — both art gallery pages get the same photo. Re-exporting from the engine saves the new array format. Session notes claiming "FP5 is stored as an array" were describing the in-memory `window.specialPhotos` structure, NOT the serialized JSON format — these differ.

### — B/I inline buttons removed from toolbar

**Decided:** Removed the B/I execCommand buttons entirely from the caption toolbar.
**Why:** `execCommand('bold/italic')` adds `<b>`/`<i>` HTML tags — visual-only in the engine preview. `stripHtml()` strips them before PDF drawing. They looked identical to the style pills, causing user confusion. Per-word bold/italic in PDF is not supported — whole-caption style via pills is the only PDF-affecting option.
**Watch out for:** Caption `innerHTML` may still contain `<b>`/`<i>` tags from before this change — `stripHtml()` handles them correctly. Do not re-add the B/I buttons.

### — FP5 art gallery photos not in PDF (Bug 3)
**Done:** `specialPhotos.FP5` is an array `[photo0, photo1]` (left=0, right=1), unlike other FP keys which are a single string. PDF export had `pool === 'artwork'` slots explicitly skipped ("Artwork slot — no photo"). Fixed `renderPage`: artwork slots now read `specialPhotos[spreadId][side==='left' ? 0 : 1]`. Fixed `pickVariantWithSpecial`: now handles array vs string for all FP types.
**Watch out for:** `pool === 'special'` (FP1/FP3/FP4) stores a single string or `{name}` object. `pool === 'artwork'` (FP5) stores an array. These are structurally different — do not unify them.

### — Plan 11-04: cover PDF + two output modes
**Done:** `renderCoverImage()` builds a 445×236mm canvas at 300dpi (18mm bleed, 3 bgColor sections, front photo slot, SVG overlay). `drawCoverCaptions()` handles front captions (centered) and spine captions (rotated 90°). Two modes via `--mode` flag: `preview` (default) → single `preview.pdf` with cover as first page; `print` → `pdf-out/print/cover.pdf` + `page-001.pdf`…`page-NNN.pdf`.
**Watch out for:** Cover caption texts were not previously exported in `book-state.json` — added `coverCaptions` field to engine export (reads from `.cover-caption[data-cover-caption-key]` DOM elements). Any old book-state.json without `coverCaptions` will silently produce no cover text — re-export from engine.

### — Cover SVG path fix
**Decided:** `renderCoverImage` uses `ASSET_BASE` (points to `assets/Template_Scribble/Spreads/`) not a separate cover base path.
**Why:** `Cover/Artboard 1.svg` lives inside `Spreads/` just like content SVGs. First attempt used a separate `COVER_ASSET_BASE` pointing to the parent folder and the SVG was not found.

### — Plan 12-01: order form FP text fields + resolution check
**Done:** `orderFormMeta` added to each FP in `scribble-data.js` (hint, placeholder, funnyWords flag for FP2). `order.html` loads `scribble-data.js` and drives `initAddonFields` from `getFpMeta(slug)` — no more hardcoded `fp2` branch. Photo count guidance added to upload section (formula: `(spreads - fpCount) * 1.5` min, `* 3` nice). Async resolution check for JPEG/PNG shows `LOW RES` badge if shortest side < 1500px. Structured `fpTexts` object (`{ fp2: ['word1','word2'], fp4: 'First steps…' }`) sent in request body and saved to Firestore.
**Decided:** `orderFormMeta: null` for FP3 (Favourite toy) — no text from customer, staff writes caption from the photo in the engine.
**Watch out for:** `getFpMeta(slug)` looks up `window.SCRIBBLE_DATA.spreads[slug.toUpperCase()].orderFormMeta`. Key is in `spreads`, NOT a separate `fpSpreadTypes` object — that name does not exist. Funny-words word list ID is now `fp-word-list-${slug}` (not the old `fp2-word-list`); add-button is `fp-add-btn-${slug}`.

### — template-engine-public.html deployed
**Done:** Copied `pages/template-engine.html` → `pages/template-engine-public.html`, committed and pushed. Now live at `https://aevia-test.pages.dev/pages/template-engine-public.html` (~1 min Cloudflare deploy). Local `template-engine.html` untouched.
**Decided:** Keep two files — local version stays the working test bed; public copy is a snapshot for demo/handover without risking breaking the local tool during order-flow integration.

### — Order form refactor: IN PROGRESS (not committed)
**Decided:** Move all FP text fields from Step 1 to Step 2 co-located with FP photo zones. FP2 (funny words) also moves to Step 2. Step 1 becomes name/email/album notes only.
**Why:** User flagged that text prompts don't belong on the details screen — they should sit next to the photo upload they relate to.
**Done so far (uncommitted):** `orderFormPhoto: { count, hint }` added to all FPs in `scribble-data.js` (FP5 gets `count: 2`). `addon-fields` div removed from Step 1 HTML. `initAddonFields` removed from init. HTML for Step 2 updated: RAW formats stripped from all `accept` attrs, `special-photo-zones` div renamed to `fp-sections`, photo count callout box added, smartphone upload tip added.
**Still needed to complete this refactor:** Replace `initSpecialPhotoZones()` with new `initFPSections()` that renders per-FP sections (photo zone(s) + text field together, using `orderFormPhoto` + `orderFormMeta`). Add `initSpecialDragDrop()` for the new zones. Update `handleSpecialFile()` to support multi-slot slugs for FP5 (e.g. `fp5-0`, `fp5-1`). Update submit validation + `fpTexts` collection for the new structure.
**Watch out for:** The partial changes are in the working tree but NOT committed — both `scribble-data.js` and `order.html` have uncommitted edits. Do not start fresh without reading current file state first. The `cancelledAddons` set still works — `cancelAddon(slug, wrapId)` is unchanged.

### — template-data.js renamed to scribble-data.js
**Decided:** Each template gets its own `<name>-data.js`. `template-data.js` implied it was the generic system file rather than Scribble-specific data.
**Why:** Prepares for multi-template scaling — pipeline code (engine, export script, csv generator) reads from the data file; new templates add a new file without touching the pipeline. Enforces the no-`if (template === 'scribble')` rule from LEARNINGS.md.
**Watch out for:** `.planning/` docs and `sessions/` logs still reference `template-data.js` — intentionally left as historical record. All live code and active docs (`CLAUDE.md`, `whats-next.md`, `customer-journey-v1.md`, `TO-DOS.md`) are updated.

### — Order form: initFPSections() completed
**Done:** `initFPSections()` and `initSpecialDragDrop()` written and committed. Replaces old `initAddonFields()` + `initSpecialPhotoZones()`. Each FP gets one section with photo zone(s) + text field co-located. FP5 renders 2 upload zones (fp5-0 / fp5-1). Validation and fpTexts collection rewritten to read SCRIBBLE_DATA directly (not URL inputType param).
**Why:** Previous session committed the function calls but not the bodies — public order form was broken (JS error on step 2). This commit fixed it.
**Watch out for:** The old `initAddonFields()` and `initSpecialPhotoZones()` remain in the file as dead code but are no longer called. Do not revive them.

### — FP3 caption field added
**Decided:** FP3 (Favourite toy) now has `orderFormMeta` with a caption field — reversed from the earlier decision to leave it null.
**Why:** User confirmed the left page has both a photo and a caption overlay. Customer knows the toy's name/story; staff can't infer it from the photo alone.

### — FP5 per-page captions (count: 2)
**Done:** `orderFormMeta` for FP5 now has `count: 2` and `labels: ['Caption — left page', 'Caption — right page']`. `initFPSections()` handles `textMeta.count > 1` by rendering separate textareas. `fpTexts.fp5` stored as array `['left caption', 'right caption']`.
**Watch out for:** Textarea IDs are `addon-text-fp5-0` and `addon-text-fp5-1` (not `addon-text-fp5`). Validation loops over `textMeta.count`. Collection stores as array, not string.

### — Photo count formula corrected
**Done:** Replaced rough `regularSpreads * 1.5 / * 3` formula with `calcPhotoTarget()` — mirrors engine's `buildBookSequence()` + slot counting. Builds actual spread sequence, picks H variant per side, counts real regular slots from SCRIBBLE_DATA.
**Why:** Original formula was wrong in two ways: (1) assumed 2 slots per spread but Scribble has many spreads with more; (2) assumed all FPs contribute 0 regular slots, but FP3/FP4 right pages pull from the main pool.
**Watch out for:** Assumes H orientation throughout (no photos at order time). Simulation of all orientation combos is a TO-DO (#46) — the H estimate may be conservative or off for portrait-heavy uploads.

### — Duplicate detection + filenames on thumbnails
**Done:** `handleMainFiles()` skips files whose name already exists in the pool. Each thumbnail shows a filename label (bottom strip, semi-transparent dark bg). Filenames are truncated with ellipsis via CSS `text-overflow`.
