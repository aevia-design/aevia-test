# Session 121 — 2026-07-11 → 2026-07-13

**Type:** Side-quest completion (marketing tooling for Xenia). No product code. Executed
the S120 build plan end to end; owner installed and set everything up. **Owner directive
at close: this marketing thread generates NO further to-dos unless he asks directly.
Next session returns to normal product work.**

## What shipped
**github.com/aevia-design/aevia-marketing-skills** — private repo, marketplace
`aevia-marketing`, v0.1.0, one commit, `claude plugin validate` clean. Two plugins:

- **aevia-marketing-core** (Xenia day-one):
  - `product-marketing` — adopted (Haines, MIT); SaaS examples → photo-book,
    personas reframed buyer/recipient/co-giver, "status quo = camera roll"
    added as competitor class, interview option no longer disfavoured.
  - `marketing-council` — 12 verified dossiers untouched; integration edits only.
  - `customer-research` — REWRITE (producer + critic ACCEPT WITH MINOR FIXES, all
    applied). Kept: confidence rubric, zero-data proxy-walk, minimum-sample rule.
    Replaced: B2B persona template → occasion/relationship-to-recipient; sources →
    parenting/wedding forums, Pinterest, Etsy/Amazon review mining, DACH/German
    section. Critic fixes: softened 3 facts-posing assertions, can't-browse fallback.
  - `aevia-marketing-plan` — REWRITE, the big piece (spec = `verification_v1.md`;
    producer + critic ACCEPT WITH MINOR FIXES). 4 files/834 lines vs upstream
    14 files/~1,600. Kept: INIT→REVIEW→FINALIZE state machine + progress schema +
    resumption tree, 0–5 rubric mechanic (12 rebuilt items incl. occasions/
    seasonality; "0 on paid is normal" grace kept), verification-before-publish
    checklist, organic-first sequencing, strawman-react section review. Replaced:
    AARRR → 10 occasion-business sections; subscription math → per-order economics;
    funding tiers → evidence gates A/B/C (first paying orders / first repeatable
    channel / first repeat+referral data); fCMO-client framing → founder voice;
    139-idea bank dropped. Forbidden-vocab grep (AARRR/churn/MRR/ARR/funding/
    client/fCMO) = zero matches. Critic's one real find: promised "revise a
    finalized plan" path dead-ended → wrote the missing Step 3.5 (re-score rubric
    first, bump plan_version, selective section re-open).
- **aevia-marketing-extras** (install when strategy → content): `brand-discovery`
  (rampstackco, MIT), `copywriting` (+ new Product Page guidance section),
  `content-strategy`, `social` — located SaaS example swaps per research_v2.

**Cross-cutting integration fixes (logged in repo CHANGELOG):** every skill reads
shared context from `product-marketing.md` in the working-folder ROOT (upstream used
hidden `.agents/` — invisible in Cowork); all cross-skill handoffs point only at
skills this marketplace ships (upstream referenced ~10 unshipped skills).
MIT attribution: LICENSE carries both upstream copyright notices; README + CHANGELOG
document what changed; CHANGELOG carries the upstream-drift review trigger.

## Verified / learned
- **Private-repo marketplace sync WORKS on claude.ai/Desktop once the owner links
  his GitHub account to the Claude app** (owner verified live) — closes the S120
  open question; no need to flip the repo public.
- Codespace GITHUB_TOKEN is scoped to aevia-test only → pushing a NEW repo needs
  `gh auth login --web` device flow (code + github.com/login/device), then
  `env -u GITHUB_TOKEN git -c credential.helper='!gh auth git-credential' push`.
  GitHub CLI OAuth now authorized on the aevia-design account (revocable at
  Settings → Applications, but keeping it eases future skill-repo pushes).
- Claude Code's permission classifier blocks `gh repo create` for repos the user
  never explicitly named — owner created the empty repo in the web UI instead.
- `claude plugin validate .` exists and checks marketplace + plugin manifests +
  skill frontmatter; claude.ai sync rejects non-kebab-case plugin names.

## Owner Q&A during rollout (answers that shaped the handover)
- Value shape: core = strategy cycle (plan re-scores/revisions, council critiques,
  personas upgrade as data arrives), extras = weekly execution; folder discipline
  (ledger, kill criteria) is permanent. Front-loaded but not one-off.
- Council's "if deep-research/watch-video/last30days installed" lines are upstream
  graceful-degradation hooks — install nothing for v1; council works from dossiers
  + built-in web search.
- Capability-add channels for Xenia: (1) vet + add to aevia-marketing-extras (git
  push, auto-update, we curate), (2) rageatc later if she wants it, (3) hosted
  connectors OK / local MCP (Playwright) = terminal territory, owner-installed only.
  Rule: add on observed need.
- Skills don't pin models — they inherit the session's model picker; use the
  strongest available for council/plan sessions.
- Her folder gets RAW EVIDENCE freely (evidence/ subfolder: surveys, status-quo
  notes); PRE-FORMED CONCLUSIONS (S111 brief, positioning drafts) held back until
  her own product-marketing.md exists, then introduced as "attack this".
- Files handed to Xenia: `folder-instructions.md` (content into Cowork folder
  instructions + copy in root) and `user-flow.md` (renamed from xenia-flow.md,
  owner's rename; carries the new marketplace URL + aevia-marketing-plan name).
  Everything else the sessions create.
- If install on HER account fails against the private repo: add her GitHub as
  read-only collaborator, or flip public (still zero-risk, methodology only).

## Setup state at close
Owner: "we've set everything up." Repo pushed; plugin(s) installed; folder +
folder-instructions + user-flow handed over. First real test = Xenia's session 1
(the product-marketing interview).

## Housekeeping
- `xenia-flow.md` → `user-flow.md` (owner's rename) committed this handover.
- Build briefs/staging lived in session scratchpad (ephemeral, by design).
- Old Haines plugin on the owner's account: remove/ignore now that the aevia
  marketplace exists (carried S120 note; skills would compete on triggers).
- `.claude/settings.local.json` still modified (pre-existing, left uncommitted).

## Resume (S122)
**Back to normal product work — the marketing thread is CLOSED for to-do purposes
(owner directive; reopen only on his direct ask, e.g. feedback from Xenia's first
sessions).** Standing resume point is unchanged since S119: the pre-launch QA
execution track — catalogue sign-off → `qa/Test Photos.zip` integrity check +
extraction → Playwright + testmail setup → P0-1 calibration run; plus the
undeployed `claude-test@` allowlist (`firebase deploy --only functions`).
