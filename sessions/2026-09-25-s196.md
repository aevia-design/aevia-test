# Session 196 — 2026-09-25

### 19:20 — English product-gallery mockups, template by template
**Done:** Captured + composed AEV-068/069/071/072/077/095 (warm-grey BG). Galleries rebuilt from owner's picks: Wander ← AEV-068 (5 spreads now; sp5 thumb added to EN+DE pages) `92031cd`; Joyride ← AEV-069 `c25ad4c`; Laguna ← AEV-095 `95b7560`; Newborn ← AEV-071 `64d540a`. Picks live in `scripts/exp2-images.mjs`.
**Next:** Scribble (AEV-072) and Papercut (AEV-077) await the owner's picks. Collections cards (`mockups/<t>/closed.webp`) not updated.
**Watch out for:** Order→template: 068 Wander, 069 Joyride, 071 Newborn, 072 Scribble, 077 Papercut, 095 Laguna. Run `compose-flat-mockup.mjs <order> <t>` BEFORE `exp2-images.mjs`, or it crops stale `front-new/back-new.png`. The flat script also writes unused `assets/images/mockups/<t>/*-new.webp` — discard them. compose-all / compose-flat each crashed once and passed on re-run (cause unconfirmed); the error shows only as a bare "Node.js v24" tail line — never trust `tail -1`.

### 19:20 — Capture hid "Add caption…" hint
**Done:** `qa/capture-spread.mjs` injects `.slot-caption:empty::before{content:none}`; empty captions printed the editor placeholder into mockups. Existing live galleries for other templates may carry it (unchecked).

### 19:20 — Open-book composer follows the curved page
**Done:** `scripts/compose-mockup.mjs` (`137714a`) bends each art column to the page's smoothed top/bottom (±30-col average, linear row interpolation) and uses the multiply-layer overhang only in the fold strip.
**Why:** Dark map frames (Wander/Joyride/Laguna FP1) showed as a wedge spilling off the paper — flat rectangle trimmed to a curved page. Map artwork itself is correct (206mm, frame ~2.5mm inside trim).
**Watch out for:** Integer outline without smoothing stair-steps straight borders. Templates not yet re-composed still show the old flat fit (invisible on pale pages).
