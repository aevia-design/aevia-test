# Session 140 — 2026-07-17 — Website copy overhaul (EN): step 1 complete

### EN copy pass finished across all 10 remaining pages
**Done:** Worked from `docs/website-copy-deltas.md` §1–7 (no re-audit — the deltas file from S139 held).
collections.html (Devotion/Radiance/Horizon cards deleted, section counts → Love 1 / Adventures 2,
CTAs → "Start your book", 4 card descriptions); all 6 product pages (story-pages header unified,
"Produced in Vienna" + 5–7 day dispatch removed, photo counts removed, Delivery → Austria-only,
4 taglines); about.html (H1 → "The opposite of a blank screen.", 2 body paras, early-access sub,
studio caption); help.html (FAQ reordered to deck sequence, 3 factually wrong bodies fixed,
placeholder Q deleted, "What if I don't like my preview?" added, contact section);
our-artists.html (Dorottya's lorem ipsum → real bio, intro/form/confirmation copy); footer sweep
across all 16 pages (tagline + "Our artists" → "For artists").
**Verified:** `node qa/copy-pass-check.mjs` — all pages clean (no console errors, no overflow, nav
centred). Only 404s are known asset gaps: Joyride mockups, Dorottya's portrait — both have
graceful fallbacks.
**Watch out for:** deltas §1–7 all marked ✅ DONE in the file itself.

### Mechanism strip: built, then dropped for good
**Done:** Built the deck's `Send your photos → preview in 48h → Pay only when you approve` strip as a
slim line in the product-page sidebar above the CTA (4 pages), then reverted all of it + its CSS.
**Decided:** Owner: sidebar version "reads poor". If ever revisited, he wants a **full-width section
below the CTA panel, above "About this template"** — reasoning: ad traffic lands directly on a
product page and needs the process visible there, not only on home.
**Why:** Dropped for good unless he raises it. Logged in deltas §2a + the EN deck (section marked
DROPPED) so no future session silently rebuilds it.
**Watch out for:** The deck contained a real internal contradiction — its italic instruction said
"under the CTA button" while its own content order showed the strip above it. That ambiguity is what
prompted the question that killed the feature. Deck layout notes stay advisory (S139 ruling).

### Owner's follow-up fixes (items 1–8)
**Done:** (3) "tap an image to preview it above" → "…preview it." on all 6 + deck; (4) story-lane
cards were 76px vs 93px depending on `.sp-desc` length → `min-height:93px` on `.sp-card` in shared
`product.css`, all uniform now; (5)+(6) orphan words on about.html `.page-title` and help.html
`.page-sub` → `text-wrap: balance` (existing site convention); (7) "why photos look softer" FAQ
rewritten to point at the **real** upload-time flow — `order.html` already flags low-res on upload
(`checkResolution` + badge) and allows per-photo swap (`removeMainFile`), so the answer now says so
instead of "we'll tell you before print" (owner: don't invite photos sent separately after,
there's no mechanism for it); (8) "Twentysix" confirmed correct, no action.
**Decided:** (1) Drive/Dropbox claim left as aspirational per owner, logged as **TO-DOS #78** with a
feasibility check — both pickers hand a File/Blob to the *existing* pipeline, so no new upload path;
Dropbox Chooser is client-side + app key only (easiest), Google Picker needs an OAuth client ID +
the narrow `drive.file` scope (no sensitive-scope review). Ship before real customers.

### "About this template" rewritten for all 6 — and a product bug fell out
**Done:** All 6 bodies rewritten: mood + layout + story pages woven in organically; the boilerplate
"We handle all design and typesetting…" cut from tender/joyride/newborn (it just restated page
mechanics); artist credited on wander + joyride only.
**Decided:** Artist is credited for **the book**, not a specific spread — "Made in collaboration with
X" at the end. Crediting the map story page read as though the artist only did that one page.
**Why:** Three rounds of owner correction, all the same root cause — **I claimed things that weren't
true.** Invented Tender's "opens with the two of you and moves through the day in order"; invented
Wander's "panoramas"; then sourced "the only book we make in landscape / 33 × 24" from Wander's own
`Print & production` spec, which is **stale placeholder text**. Owner: "we have all templates 20x20.
period." The tagline "Open landscapes" means scenery, not page orientation.
**Watch out for:** 🔴 **The product pages' spec sheets are NOT a source of truth.** Chasing that copy
claim surfaced a real customer-facing bug: **wander said `33 × 24 cm (landscape)`** (fixed this
session, owner-confirmed 20 × 20) and **newborn/scribble/papercut still say `21 × 21 cm`** — owner
says everything is 20 × 20, but he only explicitly confirmed wander, so those 3 are untouched and
logged as **TO-DOS #79 (High)**. Fix before the German build or the wrong size ships in DE too.
Cover/Paper/Binding specs came from the same placeholder block — treat as unverified.

### Process note from the owner
**Decided:** Copy proposals go **in chat for approval BEFORE editing files**. Also: keep the copy
decks current as we go — the German build works from `docs/website-copy-EN.md`, and the point is to
avoid re-litigating English copy during the DE pass.
**Watch out for:** Every S140 copy change is mirrored into `docs/website-copy-EN.md`, including the
new "About this template" section, the artist-credit rule, and the size open-question.
