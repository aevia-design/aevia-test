# Session 157 — 2026-08-07

Heirloom template build, Phase A complete and owner-approved. The most complex template yet:
4 colour variants (Beige first), 3 family monograms that select artwork, a mandatory intro,
and a two-spread add-on. Driven by `/add-template`; build state in `docs/briefs/heirloom-build.md`.

---

### Scoping — colour = registry key, monogram = data
**Decided (owner):** each colourway is its own registry entry + data file (`heirloom-beige`);
order form / engines / PDF stay colour-blind downstream of the product page.
**Decided:** monograms (Roots/Birds/Roses) live in a `monograms` block in the data file —
per-monogram cover SVG, intro SVG, clip variant, and 4 letter-caption positions.
**Watch out for:** Endalian Script is in the drop but OUTLINED into the SVGs (verified: no
`<text>`/`font-family` in any of the 39 SVGs) — never register it. IM FELL English is the
only live font.

### Stage 1 — data file
**Done:** `assets/Template_Heirloom/Beige/heirloom-data.js` from both CSVs, Tender as model.
**Why:** cover SVG is 410mm wide → authored at the 40pp 10mm spine ⇒ `referenceSpineMm: 10`
(every other template is 9). Owner later confirmed Xenia designed for 10mm. Cover opening is
a rounded-corner 80×80mm square (r 2mm); Birds/Roses art +0.5mm right of Roots → two clip
variants (`coverFrame` / `coverFrameShifted`).
**Watch out for:** the intro CSV row says "Left" but its x-coords follow right-page
convention → modelled as a right page (like Tender). Renders correctly.

### Stage 2 — fonts
**Done:** IM Fell Regular+Italic → `assets/fonts/`, @font-face ×2 engines, `FONT_FILE_MAP`.
**Watch out for:** **IM FELL English FORMS LIGATURES** (fontkit: 52 chars → 45 glyphs, both
cuts) despite being a serif — added to `LIGATURE_FONTS` (per-character PDF draw).

### Stage 3/4 — registry + smoke test
**Done:** `heirloom-beige` on all 3 surfaces (svgBase includes `Beige/SVG/`); staff dropdown
option; `qa/debug-heirloom-render.mjs` (pattern: debug-tender-render + 404 listener). Passes:
41 canvases, 0 pageerrors, 0 404s. Zones for pools story/him/her appeared with NO code change.
**Watch out for:** the engine login gate is `#eng-lock` — remove it in headless probes or
element screenshots capture the login form.

### Cover photo invisible (owner report) — two root causes
**Done:** both fixed, verified in render.
1. Xenia's export left the photo-window rect FILLED `#312128`; Tender's equivalent is
   `fill="none"`. Patched in-repo in all 3 cover SVGs (Wander precedent). **A re-export
   from Xenia re-introduces this — re-apply the patch. Check on every future drop.**
2. Cover slot 15mm high: the CSV photo row uses +3 y-bleed but the engine subtracts 18 from
   both axes; the CSV's own caption rows use +18. Data now `yMm: 98` (= artwork centre 80mm).

### Stage 4b — monogram engine logic (staff engine only)
**Done:** `getActiveMonogramDef()` (order.monogram → `#monogram-select` picker → default);
cover SVG + clip swap; 2 editable backLetter captions (bookCaptions-keyed, survive monogram
switch); intro SVG swap + 2 monoLetter captions (1-char cap); `mandatory` FPs always enter
`buildBookSequence` (intro checkbox locked); saved state carries `monogram`;
`loadOrderIntoEngine` reads `order.monogram`. Verified Roots→Roses: both SVGs + clip swap,
letters move stacked→side-by-side, land in the artwork's pockets. 312 tests green.
**Watch out for:** the monogram picker must live in its OWN `.special-zone` — first attempt
put it inside `special-zone-zodiac`, which only shows for Newborn Labour.

### Handover
**Next:** Phase B Stage 5 — order form (was just starting: Tender's wiring is at
order.html:270/579/2248). Then customer-preview + PDF parity (monogram logic must be
mirrored on BOTH; they know nothing of it yet). `order.monogram` to be written by the order
form (order-doc field like zodiacSign — the saveStaffState whitelist does NOT carry it).
**⚠ order.html changes trigger the pre-push qa:order hook.**
