# Session 41 — 2026-06-15

### — /prime + triaged next priority
**Done:** Oriented from STATUS/sessions. Dropped #60 (test-order cleanup — defer to pre-launch). Walked Evgeny through #64 (Save vs Export footgun) in plain language; **decided to defer it** — everything works, the Export-before-PDF habit is harmless, not worth a session. Chose **#47 (mobile responsiveness)** as the target.
**Why:** #47 is High and a real pre-launch blocker; mobile uploads matter because the engine auto-sorts by EXIF date (phone = happy path).

### — #47 reframed into two axes + brief written
**Done:** Ran a live mobile audit (`qa/mobile-audit.mjs`, Playwright at iPhone 13 / Pixel 7). Found: home + order-step2 responsive; **product page locked 600px, order-step1 locked 561px** (desktop shrunk-to-fit); **no hamburger on any page**. Read PRD/ROADMAP/design-principles. Wrote `docs/briefs/mobile-responsiveness.md` via /creating-briefs.
**Decided:** Two axes — **A** = responsive CSS (engine excluded, PRD desktop-only), **B** = does a mobile *browser* preserve EXIF (the real web-vs-app fork; app = Capacitor #40, a wrap not a rewrite). Bundled both.
**Watch out for:** design-principles.md already specifies the responsive system (hamburger ≤768px, product stacks) — the audit findings were violations of the existing spec, not a new design.

### — Axis A delegated to developer-agent + verified
**Done:** developer-agent implemented Axis A. Root cause turned out small: `order.html` never linked the shared `assets/css/mobile.css`, and product-stacking rules only matched the `#la` wrapper (7/10 product pages lack it). Fix = 1 `<link>` + broadened selectors. Independently verified: re-ran audit (all 10 product pages + home + order steps 1–2 report `vw==deviceWidth`, overflow 0, both devices), eyeballed screenshots, confirmed hamburger opens (nav links 1→5). reviewer-agent reviewed → **accept** (one note: documented the `!important` specificity rationale). Committed `9992b66`, pushed.
**Watch out for:** `mobile.css` is customer-facing only — never link it on staff engine / customer-preview / dashboard (deliberately desktop-only).

### — Axis B confirmed on real iPhone → STAY WEB-ONLY
**Done:** Evgeny ran a real Wander order from iPhone Safari. Layout good, upload easy, **EXIF dates survived and the engine auto-sorted correctly.**
**Decided:** The mobile browser flow is sufficient. **Capacitor app (#40) stays parked.** Don't re-litigate unless a new need emerges.

### — 4 bugs surfaced by the E2E test, all fixed
**Done + committed (`6bc871b`, `c38a5aa`):**
- **#4 (critical) PDF rotated iPhone photos** — EXIF orientation 6 (rotate 90° CW); `sharp` ignored it. Fixed with `.rotate()` in `export-pdf.js loadPhoto()` (single chokepoint). Proven on AEV-032 (dims flip 5712×4284→4284×5712); regenerated PDF confirmed by Evgeny.
- **#3 (critical) customer slot-drag** — slot `<img>` lacked `draggable=false` → native image drag hijacked the slot dragstart. Mirrored the staff engine's pattern. ⚠ **Awaiting browser confirm on a fresh non-approved order** (AEV-032 is approved → `lockForApproval` disables drag there anyway).
- **#1 price inconsistency** — home/collections showed stale €60/€120; wired both to `prices.js` (`BOOK_PRICES`).
- **#2 mobile-gate placeholder** — gate returned before the order fetch → showed literal "Order preview". Now fetches the order number from the token; verified "Your order reference: AEV-023".
**Watch out for:** any new photo-read path in `export-pdf.js` must go through `loadPhoto` or it'll skip the EXIF auto-orient and reintroduce #4.

### — Housekeeping
**Done:** Added `LINKS.md` (quick links, local + live; live uses clean URLs, customer-preview drops the token on the `.html` form). Broadened the `.claude/settings.local.json` permission allowlist (dev/qa/read-only-git no longer prompt; push/deploy still gated). Updated brief outcome banner, TO-DOs (#47 done + bugs #69–72 logged), STATUS.
