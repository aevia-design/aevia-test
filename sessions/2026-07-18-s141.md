# Session 141 — 2026-07-18 — German site: full DE build + EN/DE switcher

Committed + pushed to `main` (`c32990f`) → live on Cloudflare. Owner tests live.

### Step 0: fixed wrong book sizes (blocker), closed TO-DOS #79
**Done:** newborn/scribble/papercut said `21 × 21 cm`, horizon said `33 × 24 landscape` → all
`20 × 20 cm (square)`. Basis: owner's S140 blanket ruling ("all templates 20×20, no landscape") +
template data (`pageSize: 200` mm). horizon.html + sprout.html were **orphaned** (nothing links to
them, so the S140 copy pass never touched them) — **owner said delete**, so both removed.
**Decided (owner):** the other Print & production specs (Cover "matte linen … warm grey/ivory/slate",
Paper 170gsm, Binding, FSC, "Printed in the EU") are **all placeholders/unknowns** — kept as-is until
the **production visit (planned Aug 2026)**. Logged as **TO-DOS #80 (High)**, hard gate before launch,
mirror into DE when fixed.
**Watch out for:** don't build any copy on those specs — still unverified (S140 lesson holds).

### DE deck harmonised, then all 11 pages built
**Done:** Brought `docs/website-copy-DE.md` in line with the current EN master (EN wins every
contradiction, S139 rule): €15→**€10** Instagram discount, delivery **Austria-only** (Delivery block +
FAQ rewritten), **"Who makes it" deleted**, mechanism strip → DROPPED note, FAQ Stripe/low-res fixes,
contact + hero-caption trims, **6 new "About this template" paragraphs** translated from EN.
Then built `pages/de/` — home, collections, about, help, our-artists + all 6 product pages.
**Decided:** **separate `/de/` pages**, not a JS toggle (fits plain-HTML convention, SEO-correct real
URLs, hreflang-ready). Collection names (Love/Adventure/Kids) + "The Aevia touch" **stay English**
(brand, owner confirmed). Story-page internal `PRODUCT.fp` names stay English (feed the order pipeline);
only visible labels are German.
**Verified:** `node qa/copy-pass-check.mjs` all DE pages clean except de/our-artists + de/joyride, which
carry only the **known** 404s (Joyride mockups, Dorottya portrait) — not regressions.

### Story-page card polish — root cause was the button, not the copy
**Done:** Owner flagged German card descriptions wrapping to 3–4 ugly lines. Measured across all pages
(`/understanding-the-ask`): the real culprit was the **"Hinzufügen" button (109px)** eating ~53px of
description width vs EN "Add" (56px). Fix: button **"Hinzufügen" → "Wählen"**, added-state
**"Hinzugefügt ✓" → "Gewählt ✓"** across all 6 DE product pages (span text + `PRODUCT.labels`). That
single change levels **all desktop cards** (max 3 lines, uniform height). Two mobile 5-liners then
trimmed: Tender "Worte" `seitenfüllenden`→`großen Foto`; Newborn "Geburt" `Willkommenszeile, auf
Wunsch,`→`Begrüßung … optional`.
**Decided (owner):** "Wählen" passt. UI words flagged for Xenia's native check in the DE deck.
**Watch out for:** `product.js` got 3 EN-safe generalisations — `cfg.orderUrl` (default `order.html`),
`cfg.labels` (default Add/Added ✓), and `parseInt` page-count parse (was `.replace(' pages','')`,
breaks on "Seiten"). EN defaults unchanged. Also `product.css` `.panel{min-width:0}` — fixes a latent
grid overflow German compounds triggered (grid items default `min-width:auto`).

### EN/DE switcher (step 3)
**Done:** `.nav-lang` pill in every marketing nav (11 EN + 11 DE = 22 pages), links to the page's
counterpart (EN→`de/X.html`, DE→`../X.html`). Style added once to `assets/css/mobile.css` (shared by
all); collapses into the burger row on mobile. Verified: round-trips both ways, nav centring preserved
(0px offset), no overflow. App pages (order/account) get no switcher — no DE counterpart; `/de/` product
pages hand off to the **English** `order.html` (order flow is EN-only for now).

### Not done / deferred
- **Xenia native-check** of all German (About-this-template ×6, UI words, spec translations, Joyride
  tagline) — flagged in the DE deck's "На проверку носителем" section.
- **Order flow + emails + account still EN** — a DE customer coming from `/de/` product pages lands on
  the English order form. Whole separate localisation, not in scope this session.
- **48h preview-promise sweep** (owner's step 2) still outstanding in order flow/emails/account.
- **TO-DOS #80** — real print specs after production visit; must ship before launch, mirror to DE.
- Still-flagged uncommitted files (deliberately excluded again): `assets/Template_Joyride/*` (CSV +
  data.js), `assets/SiteFlowOpenAPI.json`, `qa/_tmp-measure-edit.mjs`.
