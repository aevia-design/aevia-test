# Session 124 — 2026-07-13

Pre-launch QA execution: **P0 complete — all four cases green.** One real product bug found
and fixed (papercut cover captions). Commit `fc45d3e` pushed to `main`.

---

### 10:26 — S1 from S122 is closed
**Done:** Owner deployed `firestore.rules` with `claude-test@aevia.at` in `isStaff()`.
Re-ran `qa/p0-1-newborn.mjs` → **clean pass, 0 findings** (AEV-053, email in ~2s, dashboard
loads and shows the order). The staff side is unblocked; every staff-side case can now run.

### 10:40 — P0-1 generalised to all 5 templates
**Done:** New `qa/p0-1-template.mjs <template>`. Orders placed in all five:
newborn AEV-053, wander AEV-054, scribble AEV-055, papercut AEV-056, tender AEV-057.
All: order + confirmation email + dashboard ✓. Console clean, no HTTP errors.
**Decided:** one **generic** special-page filler works for every template (one photo per
`[id^=dz-special-]` zone, a real option for each select, a value per text field keyed off
the id suffix) — no per-template field lists to maintain.
**Watch out for:** the S122 note *"`.addon` on scribble"* was **WRONG** — all five templates
use `.sp-card`. Photo libraries are **mixed JPG + PNG** (the form accepts both); a jpg-only
glob leaves every template except newborn short of its photo target. Wander has **no cover
photo** (`cover.slots: []` → `#dz-cover` never renders) and its country `<select>` ignores
a programmatic `selectOption()` unless `dataset.touched` is set first (an anti-autofill
guard in `onCountryPick`).

### 10:42 — 🟠 S2 REAL BUG: papercut album name truncated at 10 chars
**Done:** Typing an album name into papercut's "Front — album name" field silently cut it to
10 characters. Root cause: in `papercut-data.js` the `year` and `name` captions carried each
other's `placeholder` and `maxLength` (scribble, its twin, is correct).
**Decided:** owner fixed the **CSV** (canonical) → `year` = `2026`/10, `name` =
`Our sweet Ann`/60; `papercut-data.js` synced by hand (no generator). Verified locally: no
truncation. 186/186 tests.
**Then:** rendering the caption boxes over the cover art showed the *name* slot is the narrow
60mm box against the right cover edge — so I proposed swapping the keys/labels. **Owner's
cover art settled it: year LEFT, name RIGHT is correct**, and he chose to keep
`maxLength: 60` (album names can be long).
**Watch out for:** residual, accepted — a name over ~12 chars will not fit the 60mm slot and
wraps/overflows toward the cover edge. Mitigation is manual (engine per-caption font-size
override). Xenia should eyeball the cover on any long album name.

### 11:05 — P0-2 PASS (staff preview flow)
**Done:** New `qa/p0-2-preview.mjs <AEV-nnn> <tag>`. Engine deep-link → 51 photos load →
book **complete straight after auto-layout** (no manual design needed) → Save book state →
generate preview link → send preview → status `review_sent` → preview email → clicking the
link lands on `customer-preview?token=…` with all photos rendering. **0 findings**, on both
newborn (AEV-053) and wander (AEV-054). Resend is safe (button reads "Resend preview").
**Watch out for:** both preview buttons live on the **DASHBOARD**, not the engine (the
engine only saves). The dashboard holds an open **Firestore listener**, so `networkidle`
never fires — `goto` with `domcontentloaded` and gate on elements. Rows paint asynchronously:
wait for the order's row text, not just the first `<tr>`. Three native dialogs are in this
path — register a `dialog` handler or Playwright auto-**dismisses** (= Cancel = silent no-op).

### 11:47 — P0-3 PASS (approve → Stripe → paid)
**Done:** New `qa/p0-3-payment.mjs`. AEV-054: approve → "Pay now · €70" → Stripe test
checkout (Total due €70.00) → card `4242…` → redirect back → status **`paid`** → email
"Payment received for your Aevia order AEV-054". **0 findings.** Re-approving an approved
order is correctly a no-op (P2-9 passes early).
**Watch out for:** Stripe Checkout changed shape — it now collects a **shipping address**,
and the card fields **do not exist** until the Card row of the payment-method accordion is
opened. Its overlay button (`[data-testid=card-accordion-item-button]`) reports itself as
*invisible* to Playwright while still intercepting pointer events → dispatch the click via
`$eval(el => el.click())`. Card fields then mount in the **main** frame (not an iframe) as
`#cardNumber` / `#cardExpiry` / `#cardCvc`.
**Two owner decisions surfaced (not bugs):** (1) checkout collects a shipping address but
adds **no delivery line** — the customer pays the book price flat (delivery fee still TBD);
(2) the payment list leads with Card, **Bancontact (BE)** and **MB WAY (PT)** while **EPS —
the Austrian method — is hidden** under "More payment methods". Stripe-dashboard config.

### 12:07 — P0-4 PASS (account email vs autofill) — S109 guard holds
**Done:** New `qa/p0-4-account-email.mjs`. Created a real verified account (signup →
verification email from `noreply@aevia.at` → link → verified), then placed a newborn order
with a **decoy address injected into `#inp-email` before `onAuthStateChanged` resolved** —
exactly when Chrome autofills. Account email **won the race**, field `readOnly=true`, order
AEV-059 confirmation landed in the **account** inbox, decoy inbox **empty**, order **linked
in "My orders"**. **0 findings.** Signup+verify+sign-in all worked → **P1-5 passes too**.
**Watch out for:** the lock only applies to **verified** accounts (`user.emailVerified`);
guests keep an editable field by design.

### QA-infra gotcha (cost two hung runs)
**Decided:** `waitForEmail()` uses testmail's `livequery=true`, which **long-polls** — the
server holds the connection open until mail arrives. On an inbox that never receives
anything the `fetch` hangs and the function's own timeout **never fires**.
**To assert an inbox is EMPTY use `getEmails()` (plain snapshot) after a grace period —
never `waitForEmail()`.**

### Ruled out — correct behaviour, do not re-raise
- **Low-res badges (wander, 6/52):** true positives — 7 of the 57 wander sources are
  `_upscaled` files at 1024px short side, under the ~1575px threshold.
- **Pre-submit warning modal:** it is the **cover-orientation** guard ("wrong orientation,
  will be cropped"), tripped because the harness picks an arbitrary cover photo. Note its
  low-res count spans cover+special+main, so it can fire with **zero** badges in the main grid.
- **Dashboard `TypeError: Failed to fetch` on PDF checks:** on a clean load **all 96
  `getPdfUrl` calls return 200** (48 orders × preview+print). The errors only appear when a
  script reloads the page mid-flight — aborted requests, harness-only.
- **Brevo rewrites every email link** into a click-tracking redirect (`sendibt3.com/tr/cl/…`),
  so raw HTML never contains `customer-preview`. Tests must **follow** links, not grep them.
- **GCS `ERR_ABORTED` uploads (~50/order):** absorbed by the 3-attempt retry
  (`order.html:2255`). Still deserves the throttled-network P2 test — the rate is high on a
  fast link and a slow customer connection could exhaust the retries.
