# Session 43 — 2026-06-15

Focus: kick off the **Newborn** template (#4) build. Worked on branch `newborn-template` (NOT main — all work isolated until Evgeny approves). /understanding-the-ask + /creating-briefs run first; brief at `docs/briefs/newborn-template.md`.

### — Scope confirmed (Evgeny, S43)
**Done:** Inspected `assets/Template_Newborn/`. Newborn = square 200×200mm, lands on the renamed **Bloom→Newborn** product page (rename EVERYWHERE incl. URL `bloom.html→newborn.html` + Stripe wiring).
**Decided / key facts:**
- Custom **scalloped cover photo frame** IS present in the cover SVG as `clipPath id="i"` (the "Image" layer) — realises TO-DO #73. Heart-crop precedent applies.
- Two functional pages: **Intro** (text-only, labelled fields Name/DOB/Time/Weight/Length composed into a centred block) and **Labour** (full spread: left photo + CUSTOMER caption e.g. "Welcome to the world, Nico"; right photo + AI-generated caption (out of scope, staff-editable) + a **zodiac constellation overlay**).
- **Zodiac = NEW mechanic.** Customer picks sign (12 + None) at order time; matching overlay SVG decorates the Labour right page. Files `FP 02 {H|V} Labour Right ({Sign}).svg`; "Sagittarius"→file token "Saggit"; None = empty SVG.
- New fonts: **Twinkle Star** (cover name + spine) + **Baskervville** (cover subtitle italic + all captions).
- Cover caption examples: name="Nico", subtitle="Your First Months", spine="Our Nico".
- Optional/deferred: order-phase special-page preview (needs Scribble parity too — separate discussion). Step-based order-form UX captured in `ideas.md` (all-templates refactor, post-Newborn).

### — Stage 1 DONE: newborn-data.js (verified)
**Done:** Built `assets/Template_Newborn/newborn-data.js` (parses clean in node; all 30 page SVGs + 26 zodiac files resolve). Committed `468cfb6`.
**Decided:** SP0–SP6 photo geometry copied from Wander (identical), but caption POSITIONS taken from the Newborn CSV (they differ, e.g. SP3-right cap y141 not y179.5). All page bg white except Labour-left `#c0d5ee`. Caption color #262262, Baskervville italic 18pt. Clip path stored raw (`cover.clipShapes.coverFrame.d`) + bbox + pxPerMm (2.835) for Stage-3 transform. Cover slot = with-bleed 327/103/135/115 (validated against the measured clip bbox).
**Why:** Measured the clip bbox via a path flattener (center wb 326.7/103.0, size 134.6×113.2) — matched the CSV cover values, confirming both.

### ⚠ Stage-3 render gaps discovered (don't lose these)
- **Cover captions ignore default styling.** `renderCover` (`template-engine.html` ~1704) hardcodes weight 400 / non-italic, reads only font/size/align/color from data. The Baskervville subtitle won't be italic by default → Stage 3 must make cover-caption render honour data `italic`/`weight` (stored on the subtitle capDef). Mirror in customer-preview.
- **Custom cover clip ≠ heart.** Heart = full-canvas (200mm) CSS `clip-path` hardcoded at `template-engine.html:2348`. Newborn frame = a BOUNDED front-cover slot; path is in cover-SVG space (2.835 px/mm, trim origin, NO bleed offset). Stage 3 must translate the path into the slot's local space + scale to canvas px, then generalise `heartClip` → data-driven `clipShape` across engine + customer-preview + export-pdf.
- **Labour right page has no base-frame SVG** — its only overlay IS the zodiac SVG (bgColor white + photo + caption + zodiac). None = render no overlay.
- **Intro is a single page** (no H/V, no photo) — modelled under `pages.right.default` like a rightOnly functional. Render must not assume slots/pool.
- New engine `specialPhotos` keys needed: `labour` (2 photos) and intro has none. Today's set = {cover,FP1,FP3,FP4,FP5}.

### ▶ NEXT (Stage 2) — fonts
Download Twinkle Star Regular + Baskervville (incl. Medium Italic) into `assets/fonts/`; register @font-face in template-engine.html + customer-preview.html + font picker; add to export-pdf.js FONT_MAP; run the fontkit GSUB ligature check (per `project_pdf_font_rules`). Then Stage 3 (registry + engine render incl. #73 clip + zodiac), Stage 4 order form, Stage 5 customer-preview+PDF, Stage 6 rename, Stage 7 E2E. Plan in the brief.

### — Stage 2 DONE: fonts (verified)
**Done:** Downloaded Twinkle Star Regular + Baskervville (variable) from Google Fonts; instanced static Baskervville weights via fonttools (Regular 400, Italic 400, MediumItalic 500). Registered @font-face in template-engine.html + customer-preview.html, added font-picker entries (COVER_FONTS), and FONT_FILE_MAP keys in export-pdf.js. Committed `34dff26`. Browser smoke test: all 4 faces load, 0 console errors, render matches the cover mockup.
**Decided:** Baskervville is now a VARIABLE font on Google Fonts (has a weight axis) → "Medium Italic" (wght 500 italic) is real. Used static instances for both browser + PDF (cleaner than VF; matches existing static-ttf pattern). VF source files deleted after instancing.
**Watch out for:** All 4 fonts expose a `liga` GSUB feature → strong candidates for the fontkit ligature-gap bug (EB + Cormorant Garamond both hit it). NOT yet added to `LIGATURE_FONTS` (per playbook, only add if a real gap appears) — CHECK in Stage 5 PDF. Twinkle Star is a connected script, extra-likely. PDF cover-subtitle lookup needs style token 'mediumitalic' to hit `Baskervville_mediumitalic` (wire in Stage 5).
