# Session 44 — 2026-06-16

Focus: **Newborn template Stage 3 — registry + engine render**, on branch `newborn-template` (NOT main). Built all of Stage 3 across the staff engine + customer-preview, fixed 8 issues Evgeny surfaced on first eyeball, committed `3af559b`. 97/97 tests; Scribble + Wander render unchanged.

## What shipped (committed `3af559b`, NOT pushed — branch only)

**Stage 3 core — new render mechanics (engine + customer-preview, mirrored per engine-parity):**
1. **Registry entry `newborn`** added in all 3 surfaces (`template-engine.html`, `customer-preview.html`, `export-pdf.js`). Newborn asset base has no `Spreads/` subfolder (like Wander). Script include added in both HTML surfaces; `require()` in export-pdf.
2. **Custom cover clip (scalloped opening).** Data-driven via `cover.slots[0].clipShape` → `cover.clipShapes.coverFrame` (raw path in cover-SVG space, 2.835 px/mm). Render builds an inline `<clipPath clipPathUnits=userSpaceOnUse>` with `transform="translate(-slotL,-slotT) scale(SCALE/pxPerMm)"` to map the path into the slot's local px. **Decision (Evgeny): did NOT generalise the Scribble heart** — left `heartClip` untouched (zero regression risk), built the cover clip as a parallel data-driven path.
3. **Cover SVG edited:** the `Front › Image` group's placeholder rect (`fill="#d8eaf0"`, unique) → `fill="none"`. The opening is now transparent so the clipped photo sits BELOW the SVG (its natural z-order) and the `Illustrations` (butterflies, drawn after the opening) layer on top — matching the artwork. (Earlier attempt put the photo at z-index 3 above everything, which hid the overlapping butterflies — reverted.)
4. **Zodiac overlay** on Labour-right: `variant.zodiacOverlay` → `spreadDef.zodiac.path(orientation, sign)`, sign from `window._newbornZodiac` (set on order load from `order.zodiacSign`/`fpTexts.zodiac`). 'None' → the (None) SVG.
5. **Labour photo pool:** new `specialPhotos` bucket `FPlabour` (2 photos, one per page — indexed by side like FP5 artwork). `freshSpecialPhotos()` makes buckets template-aware (Scribble base keys + each functional spread with `orderFormPhoto`). Special-slug match made case-insensitive (`fplabour`→`FPlabour`). Orientation fallback now indexes by side so left/right pick their own H/V.
6. **Intro page** renders via the existing `textPanel` path (no new render code). **Intro replaces SP0 when selected** — data flag `replacesFirstSpread: true` + `rightOnly: true` on `FPintro`; `buildBookSequence` (both surfaces) puts the override FP at index 0 instead of SP0 (and excludes it from the body). Untick → SP0 returns. Template-agnostic (only Intro has the flag).

**8 fixes from Evgeny's first eyeball:**
- **#3 cover overlap** → the SVG transparency fix above (butterflies over photo).
- **#4 cover caption colour** → TWO bugs: (a) Stage-1 data had the bgColor `#12264b` as the font colour; corrected to CSV `#c0d5ee`. (b) the cover-caption render **never emitted `color`** — no template ever applied its CSV colour (Scribble worked by CSS-default coincidence `#493955`). Now applied in both surfaces; Scribble unchanged, Wander gets its `#3E2A55`.
- **#5 slot→slot drag** → staff-engine slot `<img>` lacked `draggable=false` (native image-drag hijacked the slot dragstart). The S41 fix was customer-side only; now mirrored to the engine.
- **#6 Labour upload UX** → tester relabelled "2 photos (1st→left, 2nd→right)" + per-side empty-slot placeholders.
- **#7 Labour-left caption position** → was the **V variant**: CSV coord was wrong (`y103` = mid-photo). Evgeny corrected the CSV (`Left V` now `x98 y183`); re-synced into the data file → caption now below the photo. (H variant was always correct.)
- **#8 font picker** → caption toolbar now scoped per template via a `fontPicker` allow-list added to each data file (Scribble: NT Somic/EB Garamond/FirstTimeWriting; Wander: Cormorant; Newborn: Twinkle Star/Baskervville). Helper `templateFonts()` in the engine.

**Local-tester scaffolding (staff engine only):** Newborn option in `#template-select`; a Labour upload zone (max 2) + zodiac `<select>` shown when Labour is ticked, wired to `window._newbornZodiac` + re-render. This mimics the order flow before Stage 4 builds the real form.

**New QA smokes:** `qa/verify-newborn-render.mjs` (cover clip / captions / labour / zodiac / intro + Scribble+Wander regression), `qa/verify-newborn-ui.mjs` (drives the Labour+zodiac tester controls), `qa/verify-newborn-fixes.mjs` (caption colour, draggable, per-template fonts).

## Verified
- Headless: cover clip applied, 3 captions, 2 labour photos, zodiac SVG loaded, intro panel; Scribble + Wander 21 spreads each; 0 page errors. Visual screenshots eyeballed by Claude (`sessions/qa-runs/shot-newborn-cover.png`, `shot-labour.png`, `shot-labour-v.png`, `shot-intro-first.png`).
- buildBookSequence: Newborn w/o Intro = [SP0,…]; w/ Intro = [FPintro,…] (SP0 gone, not duplicated); Scribble/Wander still open SP0.
- 97/97 tests after every change. **Evgeny eyeballed locally — "all works well."**

## ⚠ Watch-outs / carried context
- **Branch only, NOT pushed, NOT on main.** Cloudflare auto-deploys `main` — do not merge until the Bloom→Newborn rename (Stage 6) + E2E (Stage 7) are done and Evgeny approves.
- **`.claude/settings.local.json` deliberately left OUT of the commit** (pre-existing local change from session start).
- **Cover SVG is now edited** (`Artboard 1.svg`, `#d8eaf0`→`none`). If Xenia re-exports the cover, that transparency must be re-applied (or ask her to export the photo opening transparent).
- **Wander cover caption colour shifts** `#493955`→`#3E2A55` on next deploy (its correct CSV value, previously ignored). Subtle; intended. Mention if anyone notices.
- **Customer-preview font picker** (`CAPTION_FONTS`) is the old 3-font Scribble list — missing Baskervville/Twinkle Star/Cormorant entirely. Stage-5 parity item.
- **Labour right-page caption is AI-generated** (out of scope) — left staff-editable/blank; no customer field.

## ▶ NEXT SESSION (Session 45) — Stage 4: Newborn order form
1. **Stage 4 — order form** (`pages/order.html`, template-aware already): cover captions (name/subtitle/spine), **Intro fields** (Name/DOB/Time/Weight/Length composed into the centred Intro block — see `FPintro.orderFormMeta.fields`), **Labour** left caption + 2 photo uploads (slug `fplabour`), **zodiac select** (12 + None). Wire the payload so loadOrderIntoEngine reads it (zodiac → `order.zodiacSign` or `fpTexts.zodiac`; labour photos → `signedUrls.special.fplabour`). Realistic entry-example placeholders per page.
2. **Stage 5 — customer-preview parity + PDF.** Add Baskervville/Twinkle Star (+ scope) to customer `CAPTION_FONTS`. PDF render of the custom clip + zodiac overlay in `export-pdf.js` (only the registry/data-load is wired so far). **Run the fontkit GSUB ligature check** on Baskervville + Twinkle Star (both expose `liga`; Twinkle Star is a connected script — high risk). PDF cover-subtitle needs style token `mediumitalic` → `Baskervville_mediumitalic`.
3. **Stage 6 — Bloom→Newborn rename** EVERYWHERE incl. URL `bloom.html→newborn.html`, links, `template` param, Stripe price wiring.
4. **Stage 7 — E2E** a real Newborn order (order→engine→customer→PDF), then merge to `main` after Evgeny approves.
- Open: confirm the Labour-left V caption value with Xenia is final (currently `x98 y183` from the corrected CSV).
