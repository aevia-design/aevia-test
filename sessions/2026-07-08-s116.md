# Session 116 — 2026-07-08

## Laptop going to service — remote setup via GitHub Codespaces (side topic, no product code)

Owner's laptop (the main dev machine) goes to repair for up to a week. This session secured all work and set up a cloud dev environment so work continues from other devices.

### 1. Everything worth keeping is now in git (was the urgent part)
- **`9e19b33`** — the entire uncommitted S115 referral build (referral-utils.js, index.js, account.html, referral.test.js, s115 log, STATUS.md). 183/183 tests before commit. NOTE: this push deployed the "Refer a friend" account tab to Cloudflare; the sandbox test (incl. redeem-twice) is STILL OWED.
- **`2071bea`** — 21 previously-untracked session logs (s90–s111) + 5 briefs + `context/tone-of-voice.md` + `assets/email set-up/`.
- Still untracked on the laptop: only throwaway probe/measure scripts and temp screenshots — safe to lose.
- Owner copied the three secret files (`functions/.env`, `functions/serviceAccountKey.json`, `motif-engine/.env`) to his cloud.

### 2. Codespace created and verified
- Codespace **miniature-xylophone** on `aevia-design/aevia-test`, branch main.
- `npm install` + `npm test` = **183/183** inside it.
- Owner pasted the `.env` files into the codespace.
- `claude` CLI installed globally (`npm install -g @anthropic-ai/claude-code`) and **logged in via terminal** — terminal Claude works.
- **Browser Chrome could NOT render the Claude panel** (webview service-worker CSP error on `assets.github.dev`; cookie exceptions didn't fix it). Decision: use **desktop VS Code connected to the codespace** instead.
- Owner installed VS Code on a Mac (first Mac was too old for current VS Code) and is connecting via the "GitHub Codespaces" extension.

### 3. Personal Claude setup bundle — MIGRATION UNFINISHED, next session completes it
Personal skills/memory don't travel via git. Built `claude-setup-bundle.zip` (0.5 MB, on OneDrive Desktop), containing:
- `claude-home/` → personal `~/.claude` bits: `skills/` (checkpoint, prime, stop-slop-main, eli30, seo*, etc.), `CLAUDE.md` (global rules), `agents/`
- `project-memory/` → Claude's auto-memory for this project (goes to `~/.claude/projects/<project-slug>/memory`)
- `project-claude/` → the repo's **gitignored** `.claude/` extras: `skills/add-template`, `hooks/`, `settings.json`

Owner uploaded it to the codespace — **Mac Safari had auto-unzipped it**, so it landed as a folder (exact location unclear; owner couldn't find it). **The install commands were never run.**

**▶ For the Claude session inside the codespace — finish the migration:**
1. `find / -maxdepth 4 -name "claude-home" -type d 2>/dev/null` (or look for `claude-setup-bundle*` under `/workspaces/aevia-test`) to locate the uploaded bundle.
2. From the bundle root:
   - `cp -r <bundle>/claude-home/. ~/.claude/`
   - `mkdir -p ~/.claude/projects/-workspaces-aevia-test && cp -r <bundle>/project-memory ~/.claude/projects/-workspaces-aevia-test/memory` (verify the actual project-slug dir name Claude uses there; adjust if different)
   - `cp -r <bundle>/project-claude/. /workspaces/aevia-test/.claude/`
   - remove the bundle folder from the repo working tree (it must NOT be committed)
3. Restart Claude; verify `/checkpoint` + `/prime` appear and project memory recalls (e.g. "owner tests on live Cloudflare site", engine-parity rule).
4. Note: rageatc plugin skills (`/handover`, `/solutioning`) are NOT in the bundle — reinstall the plugin in the codespace if wanted, or write handovers manually (STATUS.md + sessions/ file, end by naming the completed session number).

### Product work — unchanged, next after migration
Referral sandbox test (S115 plan): account → Refer a friend → code → second-email order redeems (−€10) → pay 4242 → THANKS- email; **crucially redeem the same share code twice with the same referee email** (first_time_transaction guest gap). Then decide on option (b) if unblocked.
