# Session 94 — 2026-06-30

## Topic
New **flat front/back 3D book mockups** for product pages (Xenia's new views), built by a reusable tool — NOT hand-made per order. All work is in throwaway scripts + review images; **nothing committed, nothing wired into the website.**

## What happened
- Xenia supplied two new mockup views. First tries used the **SVGs** (`assets/mockup example/new/{front,back}.svg`) — wrong: the SVG export flattens cover+lighting, so swapping its layers makes a flat sheet (lost spine/edges).
- Xenia then supplied **layered PSDs** (`assets/mockup example/new/{front,back}.psd`) — the correct source. Layers: `Main Image` (cover artwork = swap target), `Layer 1` (multiply) + `Highlight` (screen 15%) = lighting, `111` = book body/spine/edges, `Shadow`+`BG` = backdrop.
- Built a compositor (pure `ag-psd`+`sharp`, no browser, no egress) that reads the PSD, swaps `Main Image` with an order's cover face, re-applies the lighting overlays.
- **Newborn (AEV-039) FINISHED + owner-approved**, incl. a fix to preserve the **light-blue spine + gradual transition** (don't overwrite Xenia's neutral spine band; feather-mask the swap on the spine side — front=left, back=right). Files written:
  - `mockups/AEV-039/{front,back}-new.png`
  - `assets/images/mockups/newborn/{front,back}-new.webp`
- **Cross-template recolor** (since Xenia won't hand-make each — the whole point of the tool): reuse the newborn shell, recolor the spine to each template's `mockupEdges.spine`. Method evolved RC1→RC4. **RC4 is the good one**: spine built from the NEUTRAL `Main Image` band (no butterfly ghosts) recolored `spineColor × (lum/239)`, feathered. See memory `project_svg_flat_mockups` for exact params.
- Generated RC4 **front** previews for **wander / tender / scribble** in `temp-screenshots/mockup-test/RC4-*.png`. They looked clean (correct green / cream / yellow spines, full covers, no ghosts).

## State / where things are
- Cover sources (already local, no egress): `sessions/qa-runs/cover-wrap-AEV-{039,040,041,043,044}.png` ([back|spine|front] trim wrap, 3681×1803).
- Spine colors: wander `#86A37B`, scribble `#fdd16f`, papercut `#8bb8d8`, tender `#fbf8f6`, newborn `#c0d5ee`.
- All recolor logic lives in **throwaway scripts that were deleted** — rebuild from memory `project_svg_flat_mockups` (it has the full recipe + params).

## ▶ NEXT SESSION (Session 95)
1. **Owner has NOT inspected the RC4 previews yet** — start by opening `temp-screenshots/mockup-test/RC4-wander-front.png`, `RC4-tender-front.png`, `RC4-scribble-front.png` (view as small JPEGs — 2000px PNGs exceed the 32MB image API limit; downscale to ~1100px JPEG first).
2. If approved: rebuild the recolor compositor (per memory), and produce **all 4 templates × front+back** (wander/scribble/papercut/tender). **Papercut front was never previewed; no backs were built yet.** Backs: spine on the RIGHT, mirror the rx direction, source from the right neutral band.
3. Write outputs to `mockups/AEV-0xx/{front,back}-new.png` + `assets/images/mockups/<template>/{front,back}-new.webp`. (Newborn already done — don't regenerate.)
4. Check the thin outer book edge (x<935, left of Main Image) — it stays as newborn scene (light blue); confirm it doesn't read odd on non-newborn books. Minor: "between" sits close to the spine on Wander.
5. Then **product-page wiring** (the original goal): fold front-new/back-new into the galleries; decide replace vs add alongside existing `closed.webp`/`hero.webp`.

## ⚠ Watch-outs
- **Nothing committed; website untouched.** New files only in `mockups/` + `assets/images/mockups/*/` + `temp-screenshots/`.
- Don't source spine shading from the scene composite (newborn butterfly ghosts) — use the neutral Main Image band (RC4).
- Newborn used a different (composite-gradient) spine method and is approved — leave its files alone.
- Per-template PSDs do NOT exist and won't — the recolor tool is the path.
- Lots of throwaway files this session in `temp-screenshots/mockup-test/` (orig/FINAL/REBUILD/RECOLOR/RC*/SPINE/DIAG/MI/PROBE/CONTACT) — gitignored area, clean up later.
