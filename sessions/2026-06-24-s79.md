# Session 79 — 2026-06-24

Continued the **Tender** template build via `/add-template`. All work UNCOMMITTED working-tree.
Build state checklist: `docs/briefs/tender-build.md`.

## Phase A — engine render (SIGNED OFF)
Two eyeball bugs fixed:
1. **Spine font colour** — `tender-data.js` had spine `color:#7c746e` (taupe), but the cover
   CSV's per-caption `captions_1_fontcolor` for the Spine row is cream `#fbf8f6` (it sits on
   the dark `#8a817a` spine band). Root cause: hand-authoring applied one uniform colour to all
   cover captions; the cover CSV has SEPARATE colour columns per caption. Fixed + skill trap added.
2. **No upload field for Our-story / Words functional photos** — the staff engine's special-photo
   upload UI + slot resolver were hardcoded to Newborn/Scribble ids/pools (`special/artwork/labour`).
   De-hardcoded in `template-engine.html` (3 edits): (a) slot resolver treats ANY pool except
   `regular`/`cover` as special, per-side when `orderFormPhoto.count>=2`; (b) new
   `ensureFunctionalPhotoZones()` builds an upload zone for every functional spread with
   `orderFormPhoto` lacking a static zone, `updateSpecialPanel()` toggles generically; (c) upload
   handlers → event delegation on `#special-panel`. Verified headless: zones appear, accept uploads,
   photos resolve, 0 pageerrors.
3. **Page border/frame** (Evgeny request — pale Tender pages blended into the engine canvas).
   Added to `.page-canvas` in BOTH engine + customer-preview: `1px solid rgba(0,0,0,0.08)` hairline
   + `border-radius:2px` + soft two-layer shadow. Template-agnostic (subtle on colourful templates).

## Phase B
### Stage 5 — order form (done)
- Registered Tender in `order.html` (script tag + `tender:` registry entry).
- Our-story/Words are the same pattern as Intro (labelled fields → composed text), so routed them
  through the existing `introFields` mechanic (renamed their `orderFormMeta` flags
  `storyFields`/`wordsField` → `introFields`).
- **De-hardcoded `composeIntroBlock`**: each Tender functional spread carries its own
  `compose(values)` in `orderFormMeta`; `order.html` calls `(textMeta.compose||composeIntroBlock)(vals)`
  so Newborn's birth-story default still applies. Added `orderFormMeta.heading` (renderIntroFields
  shows it).
- Verified headless (Tender order URL): intro(4)+story(photo+2)+words(photo+1) render, headings
  correct, dropzones work, `validateSpecialStep()` ok, compose blocks correct, 0 console errors.

### Stage 6 — customer-preview parity (done, not live-verified)
- Mirrored the engine resolver generalisation into `customer-preview.html:1834`.
- Confirmed already-correct: `freshSpecialPhotos` data-driven (story/words buckets), Parisienne in
  `CAPTION_FONTS`+@font-face (S78), regular-photo assignment short-circuits FP pages with no regular
  slots (`hasRegularSlots`), text-panel render flag-agnostic. Composed intro/story/words text reaches
  the customer via SAVED captions (engine `loadOrderIntoEngine` seeds `fpTexts[slug]` into the panel's
  actual DOM side: story→right, words→left, intro→right).
- Unified leftover render-side textPanel flags `storyFields`/`wordsField` → `introFields` (decorative).
- ⚠ Not driven with a LIVE order (token-gated) — full customer render eyeball at Phase C E2E.

### Stage 7 — PDF parity (code-complete, awaiting Evgeny's dashboard render)
- **No Tender-specific code changes needed.** Cover ellipse clip uses the generic
  `clipShapes.coverFrame` path (export-pdf.js:903); `fullBleed`, `overlayBelow`, Parisienne
  (`FONT_FILE_MAP`+`LIGATURE_FONTS`), generic `textPanel` render all wired in S78. The Cloud Run
  renderer's slug→spread map resolves `fpstory`→`FPstory` / `fpwords`→`FPwords` via its general branch.
- Confirmed renders 40 pages with 0 errors — but NOT visually verified.

## Pricing (decided)
Tender = standard €70/€100, same as all templates (shared `assets/js/prices.js`, no per-template
values). Created `docs/pricing.md` pointing at `prices.js` as the canonical source.

## Order-form copy fix
`love` album-note placeholder changed honeymoon→wedding ("Our wedding day — 14 June 2026, Vienna…").

## ⚠ Process slip — local PDF render caused egress (corrected)
Off a vague "let's go with stage 7", I rendered AEV-044's PDF **locally** (temp script calling
`generatePdfFromFirestore`), which downloads the order's full-res originals from GCS = real egress on
Evgeny's bill — exactly what chunk-024's in-region renderer exists to avoid. Removed the temp script;
left the 177 MB PDF (egress already spent, Evgeny may inspect/delete). Hard rule now added to the
`/add-template` skill (PDF stage ⛔ + traps) and saved as memory `feedback_no_local_pdf`: **never render
a PDF locally without an explicit per-render go-ahead; PDF generation is Evgeny's job via the dashboard.**

## State for next session
- Tender Phases A+B mostly done; **Stage 7 awaits Evgeny's dashboard PDF eyeball of AEV-044**.
- Then Phase C: Stage 8 product page Vows→Tender + Stripe + `docs/templates.md` update; Stage 9 E2E;
  Stage 10 merge (backend-first) + handover.
- Everything uncommitted. 122/122 tests throughout.
</content>
