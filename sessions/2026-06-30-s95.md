# Session 95 — 2026-06-30

## Topic
Continue flat front/back mockups (S94). Tackle scribble. Ended in a **proper brief** after repeated brute-forcing failed — owner stopped me and demanded a principled, planned approach. Nothing wired to the website; newborn approved files untouched.

## What happened
- Compared S94's RC4 scribble front vs approved newborn: spine was a wide flat band bleeding into the cover face (RC4 tuned for newborn's dark cover; on scribble's cream cover the luminance-based mask over-painted).
- Tried multiple fixes (all wrong, all reverted in logic): tint `111` body layer (painted whole book), narrow positional strip (flat rectangle, no 3D), synthetic spine strip (detached floating bar). Owner correctly called out brute-forcing + a premature "front is excellent" on a still-broken (detached spine) image.
- Invoked `/systematic-debugging` then dug into the PSD per-layer. **Decisive architecture finding** (see memory `project_svg_flat_mockups`): `Main Image` = white cover-face placeholder (swap target); `111`/empty-named layer = the FULL baked newborn book sitting ABOVE it, **with the spine #c0d5ee baked in** (correct curve + shading). `Layer 1` multiply + `Highlight` screen = lighting; `Shadow` linear burn; `Cover` op 0.
- **Root cause of every failure:** recolored the spine in the FLAT `111` coordinate space, which doesn't align with the 3D-lit composite → detached strip. Fix must work in the **approved newborn render's coordinate space**.
- Owner clarified the spine is TWO parts: (1) spine face color (hue-swap), (2) hinge-shadow/indentation crease that must be **approximated** per template (bluish on dark newborn, neutral on light scribble). Agreed approach: **darken the cover edge by a factor (multiply), not tint a color**.
- Ran `/understanding-the-ask` → `/creating-briefs`. Wrote **`docs/briefs/flat-mockup-compositor.md`** capturing: fixed-scene model, two-part spine, the coordinate-space bug, order-independence (must work if owner changes a newborn order's photos), lighting-transfer risk, and a requirement to capture lessons in memory.

## State
- **Brief written, NOT implemented.** `scripts/compose-flat-mockup.mjs` exists but is BROKEN (detached spine) — rewrite per brief, don't trust it.
- Newborn approved files correct + untouched: `assets/images/mockups/newborn/{front,back}-new.webp`.
- Scribble outputs in `assets/images/mockups/scribble/` are BROKEN test artifacts — ignore/overwrite.
- Lots of throwaway probes in `temp-screenshots/mockup-test/` (gitignored).

## ▶ NEXT SESSION (Session 96) — IMPLEMENT the brief
1. Read `docs/briefs/flat-mockup-compositor.md` + memory `project_svg_flat_mockups` first.
2. Build the compositor per brief: newborn render as geometric base; swap cover face (carry baked lighting); recolor spine face (aligned to composite coords, full height, attached); approximate hinge crease via edge-darken factor. Verify against approved newborn + pixel probe — NOT a zoom crop.
3. Confirm order-independence (re-run newborn with a different order → correct).
4. Roll out wander/papercut/tender. Then product-page wiring (separate step).
5. Capture the closed/hero first-round lessons in memory (still owed).

## ⚠ Watch-outs
- **Don't revive RC1–RC4 or the synthetic strip** — all wrong (flat-coordinate recolor).
- **Never call a zoom crop "success"** — verify full image + spine pixel probe against the newborn reference.
- Hinge-crease approximation + lighting-transfer onto a light face are the two real unknowns (see brief Known Risks).
- Per-template PSDs do not exist; the newborn PSD shell is recolored for all.
- `.claude/settings.local.json` left out as usual.
