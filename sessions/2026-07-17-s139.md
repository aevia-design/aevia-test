# Session 139 — 2026-07-17 — Website copy overhaul (EN)

### 15:41 — EN copy deck finalised as master
**Done:** `docs/website-copy-EN.md` (Xenia's pass + owner edits) is now the authoritative copy source; stop-slop ran flag-only, owner approved 4 fixes (contact filler cut, Stripe "industry-standard" cut, artist-form confirmation reworded, Instagram line → "DM us, we read everything."), rejected the rest. Delivery placeholders filled: dispatch 2 business days, Austria delivery 7.
**Decided:** EN file wins every EN/DE contradiction (Austria-only delivery, €10 not €15 Instagram discount, no "Who makes it" block). DE harmonisation is step 4, later.
**Why:** Owner edited EN by hand after Xenia's review; DE predates those edits.
**Watch out for:** Both copy files are still untracked in git — commit them with the implementation. DE file still contains the deleted "Who makes it" block and stale EU-delivery/€15 facts.

### 15:41 — Session plan and owner decisions
**Decided:** Execution order: (1) EN copy + structural changes, (2) modifier-agent sweep 24h→48h preview promise (order flow, emails, account), (3) EN/DE language switcher in header, (4) DE harmonise + build. Placeholders for all missing photography (hero rotation, book tour, About studio series — shots don't exist yet). Instagram €10 line stays. Sprout/devotion/radiance/horizon pages stay on disk, unlinked.
**Why:** Owner's explicit sequencing; imagery arrives later from photo shoots.
**Watch out for:** Artist form already posts to partners@aevia.at (`functions/email.js:36`) — copy-only change there, don't touch wiring. /designing-interfaces skill self-scopes to apps NOT marketing pages — use `context/design-principles.md` for the new home blocks instead.

### 15:41 — Audit: monolith died, 3-way split worked
**Done:** Full audit of deck vs live pages, reports delivered in-chat (home; collections+products; about/help/artists+footer).
**Decided:** One big audit agent (15 pages) died silently after 20+ min — task ID vanished, no notification. Re-ran as 3 parallel scoped agents; each finished in ~90 s.
**Watch out for:** Background agents can die without any signal — poll with TaskOutput (block:false) if >10 min quiet instead of waiting on the completion bell.

### 15:41 — Audit findings that gate implementation
**Done:** Key facts for the edit pass: home hero currently rotates TEXT (deck wants static text + rotating photos + 2nd button) — hero JS needs rewrite, partial deletion throws (`goSlide`/`heroTag` refs). Testimonials block (5 fake reviews + €15 voucher) deleted entirely → replaced by early-access letter. Two net-new home blocks: "Inside every Aevia book" scroll tour + early-access letter. help.html FAQ says "we deliver across the EU" — factually wrong NOW, must flip to Austria-only. our-artists.html Dorottya bio is lorem ipsum; deck has the real bio (closes a Joyride merge gate from STATUS). Footer tagline wrong in 2 variants across 16 inline copies; "Our artists" label → "For artists". Nothing is templated — all edits are per-file. home.html has NO meta description (add, not swap). BOOK_PRICES p40=70 confirmed; collections markup hard-codes stale "€ 60" (cosmetic, fix anyway). Product pages: mechanism strip is the only new build; newborn's "Special spreads" header → "Story pages. All free." like the others.
**Watch out for:** Implementation NOT started — owner hadn't yet said "go" on starting home.html when checkpoint was taken. Full audit detail lives in this chat's transcript, not in files.

### 08:05 — home.html rebuilt to the deck (DONE, verified)
**Done:** All deck copy applied; hero inverted (static headline, rotating photos only, 2 buttons, 48h reassurance line); testimonials block + its CSS deleted; two new blocks built (`.inside` pinned tour, `.early` letter); Roman numerals I–V on steps; meta description added; stale "€ 60" markup → € 70; footer tagline + "For artists" relabel.
**Decided:** Deck's serif-body instruction for the early-access letter IGNORED — owner ruled design decisions are ours and follow `context/design-principles.md` (serif is headings-only). Instruction deleted from the md deck. Hero rotation captions ("Wander, for the roads…") deleted at owner's request, also removed from the deck.
**Why:** Owner: "design decisions are owned by you and in accordance to the rules we developed."
**Watch out for:** `/designing-interfaces` self-scopes to apps/dashboards, NOT marketing pages — the correct yardstick for these pages is `context/design-principles.md`. Card/block headings carry `text-wrap: balance` to kill orphan words — keep it on new headings.

### 08:08 — Nav links were 65px off-centre on ALL pages
**Done:** Fixed in `assets/css/mobile.css` (shared by all 16 pages) with a `min-width: 769px` block giving `.nav-logo` and `.nav-actions` equal flex basis.
**Decided:** Fixed once in the shared stylesheet rather than 16 inline edits.
**Why:** `.nav` is `space-between` across logo | links | actions, which centres the middle only if both sides match. Logo 113px vs actions 244px → half the 130px difference = the exact 65.3px measured offset.
**Watch out for:** Scoped to desktop deliberately — below 768px the nav wraps into the burger and depends on flex `order`, which `flex: 1 1 0` would break. Verified 0px offset on home/about/tender/collections/help.

### 08:10 — "Inside every Aevia book" = pinned scroll tour
**Decided:** Section is 3 screens tall, content `position: sticky` pins to viewport, image+text panes crossfade per screen, 3 progress marks. Owner asked for this over plain scrolling. Earlier/simpler variants (plain rows; sticky image + scrolling text) rejected by him.
**Why:** Owner wanted to "stay inside the section" while content swaps.
**Watch out for:** Native scroll is NOT hijacked (no wheel interception) — keep it that way; scroll-jacking breaks trackpad flicks, PageDown, and screen readers. Mobile (<768px) collapses to plain stacked panes via CSS and the JS is gated behind `matchMedia('(min-width: 769px)')`. Section costs ~2 extra screens of scrolling; shorten `.inside-scroll { height: 300vh }` if it feels slow once real photos land.

### 08:12 — QA scripts consolidated
**Done:** Four throwaway `_tmp-*` scripts replaced by `qa/copy-pass-check.mjs <page…>` — checks console errors, horizontal overflow, nav centring, and shoots desktop+mobile screenshots. Defaults to all 11 marketing pages.
**Why:** The next session verifies 10 more pages the same way; S94's lesson was that deleting throwaway scripts loses the method.
**Watch out for:** `qa/_tmp-measure-edit.mjs` (S136, untracked) is still sitting there — not mine, still unadopted.

### 08:30 — Audit deltas written to a file (post-handover)
**Done:** `docs/website-copy-deltas.md` — every per-page delta (current string → deck string), deletions, new builds, and the 16-page footer list, transcribed from the three S139 audit reports while they were still in context. STATUS + CLAUDE.md now point at it.
**Decided:** Wrote the file instead of letting the next session re-audit.
**Why:** Owner opened S140, saw STATUS say "the deltas live in the chat transcript, re-audit before editing", and asked for a file so the audit isn't paid for twice.
**Watch out for:** Line numbers in that file are from the pre-edit audit and drift as pages are edited — grep the quoted "current" string instead. The file's claims were true at S139; if a page looks different, trust the page.
